# Semantic Changes Ledger — Plan 023

> **Rule:** every PR that lands on `refactor/decomp-v2` adds one or more rows here before merge. Phase 6 totals the rows to pick the version number for `master` → `main`.
>
> **Starting from:** `3.34.5` (master, 2026-05-13)

## Categories
- `breaking` — user-visible behavior changes, removed features, public API surface changes (Supabase tables, companion HTTP API, exported message types). One of these forces a MAJOR bump.
- `feature` — new user-visible capability or new internal capability another app could consume. Drives MINOR.
- `internal-schema` — change to a `chrome.storage` key shape or migration that future tools must account for. Drives MINOR.
- `fix` — bug fix with no other behavior change. Drives PATCH.
- `perf` — performance / efficiency improvement with no behavior change. Drives PATCH.
- `internal-only` — pure refactor, no observable effect at any boundary. **No version bump.**

## Bump rules (Tabatha convention)
- Any `breaking` row → MAJOR
- Else any `feature` or `internal-schema` row → MINOR
- Else any `fix` or `perf` row → PATCH
- Only `internal-only` rows → no bump (still ship as an `-α` if helpful for distribution)

---

## Ledger

| Date | Phase | Task | Change | Category | Notes |
|---|---|---|---|---|---|
| 2026-05-14 | 6 | 06-router-finalization (version pick) | Tallied the ledger and bumped Tabatha from `3.34.5` → **`4.0.0`** (user override of the rule-based MINOR pick of `3.35.0`). Rule-based tally: `breaking` 0, `feature` 1 (clockTickService), `internal-schema` 3 (01-foundation, 03-data-services, 04c-task-service), `perf` 4 (04a-tab, 04b-focus, 05a-group/blockgate, 05b-companion-backoff, 05c-alarm-perf), `internal-only` 6 (02, 04d-clockService, 05b-companion-move, 05c-alarm-consolidate, 05d-router, 06-flag-cleanup). The MAJOR override marks the first cumulative release going to `master` since v3.0.0 — this PR ships ~35 minor versions of integration-branch feature work (v3.17.x → v3.34.5) PLUS the Plan-023 backend decomposition in one drop. Also removed the transitional `serviceFlags.focus.ready` cross-service stub from `tabService` and the matching `services: { focus: { ready: true } }` injection in `background.js`; tabService now delegates `autoQueueFromIntent` and `linkTabToFocus` directly through injected `focusService` deps. | `internal-only` | Behavior preserved: with `ready: true` already shipping in PR #11, the injected `focusService` paths were already canonical; this removes the dead local fallback bodies and the dormant flag. `updateTabIntent` argument was already ignored by the injected version, so dropping it is no-op. Version pick deviates from the ledger rule because master is at v3.0.0 and this release ships everything since cumulatively. |
| 2026-05-14 | 5/6 | 05d-router-finalization | Collapsed `background.js` into a 171-line service orchestrator, removed `handleLegacyMessage`, moved tab activation tracking into `tabTrackingService`, idle/off-Chrome handling into `clockService`, notification click/button handling into `notificationService`, URL-lock navigation into `tabService`, focus/sync alarm registration into owning services, and Supabase debounce/sync into new `syncService`. | `internal-only` | Runtime request/response shapes preserved; unknown messages still return `{ error }`. Build green; exactly one `chrome.alarms.onAlarm.addListener` remains. |
| 2026-05-14 | 5 | 05c-alarm-service | Consolidated three `chrome.alarms.onAlarm` listeners (two in `background.js`, one in `bootstrap.js`) into a single dispatcher in `alarmService`. Each alarm name now routes to a handler exposed by the owning service: `focusService.handleFocusTimerExpired` / `handleUnfocusedNudge`, `tabService.handleContextTimerExpired`, `notificationService.handlePomodoroComplete`, `clockService.handleIdleAutoBreak`, `sessionService.saveSessionSnapshot` / `exportMarkdown`, `bootstrap.runRetentionCleanup`. `idleAutoBreakApplied` state moved into `clockService` (consume/reset). | `internal-only` | Public request/response shapes unchanged. `alarmService` exposes no runtime message handlers. |
| 2026-05-14 | 5 | 05c-alarm-service | Removed the duplicate `chrome.idle.setDetectionInterval(60)` call from `background.js`. Auth-guarded the `supabase-sync` alarm so dispatch is skipped entirely when no Supabase session exists, rather than entering `syncToSupabase` and bailing inside. | `perf` | Idle detection cadence unchanged (60s, configurable via `settings.idleThresholdMinutes` through `settingsService`). |
| 2026-05-14 | 5 | 05b-companion-service | Moved the desktop companion WebSocket bridge into `companionService`, deleted the self-instantiating `companion-bridge.js`, registered companion runtime messages in the service router, and made bootstrap explicitly initialize the WebSocket lifecycle. | `internal-only` | Existing companion message shapes are preserved; `COMPANION_CLOCK_BREAK` is accepted as an alias for the existing `COMPANION_TOGGLE_BREAK` behavior. |
| 2026-05-14 | 5 | 05b-companion-service | Added transition-only `COMPANION_IDLE_STATE` broadcasts, moved companion WebSocket/heartbeat/reconnect constants into `constants.js`, and changed reconnect scheduling to exponential backoff capped at 30 seconds. | `perf` | Idle broadcasts now fire on state changes instead of repeated desktop idle detections. |
| 2026-05-14 | 4 | 04d-clock-service | Extracted `CLOCK_IN`, `CLOCK_OUT`, `TOGGLE_BREAK`, `GET_CLOCK_STATUS`, `GET_CLOCK_HISTORY`, and `GET_LAST_SESSION` into `clockService`; added `endBreakIfActive()` for focus resume and kept companion/webhook side effects behind injected dependencies. | `internal-only` | Public clock request/response shapes are preserved. `handleLegacyMessage(message)` stays one-arg after the rebase because remaining legacy companion cases do not use `sender`. |
| 2026-05-14 | 4 | 04d-clock-service | Added `clockTickService` with `TICK_SUBSCRIBE`, `TICK_UNSUBSCRIBE`, and `GET_TICK_STATUS` so extension pages can share a singleton 1Hz tick broadcaster. | `feature` | The service now supports subscriber-counted ticks; UI migration from per-component intervals is deferred. |
| 2026-05-14 | 5 | 05a-group-blockgate | Extracted groupService and blockgateService from the background router; moved Chrome tab-group lifecycle sync into groupService; capped sugarBox through `settings.storage.sugarBoxCap` with archival before eviction; added one-shot `PARKED_TABS_WARNING` broadcast when parked tabs reach the configured warning threshold. | `perf` | Public request/response shapes for migrated group and blockgate handlers are preserved. New storage cap default is additive (`sugarBoxCap=500`); dropped sugarBox entries archive under `_archive_sugarBox`. |
| 2026-05-14 | 4 | 04c-task-service | Extracted taskService from the background router for task CRUD and stage-gating ownership; archived org tasks now carry `archivedAt` and tasks older than `settings.storage.archivedTasksColdAfterDays` move from `tabathaOrg.tasks` to `_archivedTasks`. | `internal-schema` | Public request/response shapes for `GET_TASKS`, `CREATE_TASK`, `UPDATE_TASK`, and `DELETE_TASK` are preserved. `_archivedTasks` is an internal cold-store key for old archived task records. |
| 2026-05-14 | 4 | 04b-focus-service | Extracted focusService from the background router for focus lifecycle, funnel transitions, intent-task linking, and intent merge ownership; `SET_FUNNEL_STAGE` and `UPDATE_FOCUS` now share the same stage transition helper; completed focus history is archived through `archiveBeforeCap` before applying `settings.storage.focusHistoryCap`. | `perf` | Public request/response shapes for migrated focus handlers are preserved. Dropped focus history entries now land in `_archive_focusEngine.history` instead of being silently discarded by the cap. |
| 2026-05-14 | 4 | 04a-tab-service | Extracted tabService from the background router for tab CRUD/context/intent/close handlers and tab lifecycle listeners; on tab removal, InBar notes are archived into `closedContexts`, `inbarNotes[tabId]` is pruned, per-tab time is aggregated through tabTrackingService, and `_legacyTasksBackup` is removed by a one-shot cleanup. | `perf` | Public request/response shapes for migrated tab handlers are preserved. Focus bridge calls are fallback-capable until focusService is merged. |
| 2026-05-14 | 3 | 03-data-services | Extracted tabTrackingService, categoryService, sessionService from the background router; replaced hardcoded `.slice(0, 500/50)` caps with `enforceArrayCap` + `archiveBeforeCap` for `intentHistory` / `closedContexts` / `sessions`; merged `intentChangeLog` into `intentHistory` with the union shape (one-time migration, legacy key removed); snapshot alarm is now user-tunable via `settings.storage.snapshotIntervalMinutes` (re-armed on UPDATE_SETTINGS); `chrome.tabs.onRemoved` now aggregates `timeTracking.byTab` into `byGroup`/`bySubGroup`/`byProject` before deleting the per-tab row. | `internal-schema` | `intentChangeLog` is removed and merged into `intentHistory` — any external reader of `intentChangeLog` must read `intentHistory` instead. Cap defaults are still `intentHistoryCap=500`, `closedContextsCap=500`, `snapshotCap=20`. Response shapes for migrated handlers preserved; `GET_LATEST_SESSION` is now handled by sessionService (same `{ session }` shape). |
| 2026-05-14 | 2 | 02-notification-settings | Extracted notificationService and settingsService from the background router; scoped outbound broadcasts to extension-only vs InBar-relevant all-target messages; added validation for `settings.storage` writes. | `internal-only` | Behavior and response shapes preserved for migrated handlers except invalid `settings.storage` writes now return `{ error }` before persisting. |
| 2026-05-14 | 1 | 01-foundation | Extracted constants/helpers/bootstrap from background.js; added router skeleton (`services[]` + `handleLegacyMessage` fall-through); added `storage` block to `DEFAULT_SETTINGS` with additive migration; added `enforceArrayCap` + `pruneStaleKeys` to storageService; added `archiveService.archiveBeforeCap` primitive. | `internal-only` + `internal-schema` | Settings migration is additive (defaults seeded only when missing). `services.length === 0` after this task — services land in Tasks 02+. Cap defaults match prior hard-coded values (logsCap 500, focusHistoryCap 200). |
| | | | | | |

<!-- Append rows above this line. Keep newest at top, oldest at bottom. -->
