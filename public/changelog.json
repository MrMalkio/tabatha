{
  "generatedAt": null,
  "releases": [
    {
      "version": "6.7.55",
      "title": "Security: HTML-escaping across gatekeeper.js + inbar.js (audit NOW #1)",
      "date": "2026-07-21",
      "intro": "> Closes an XSS gap flagged in the 2026-07-21 security audit: 37 call sites across gatekeeper.js (Intent-Popup/InPop) and inbar.js interpolated user-controlled strings (focus/intent labels, notes, pause text, checkpoint/backburner reasons) into `innerHTML`-style templates with no escaping. Since these labels/notes already sync to Cloud Sync and are already readable cross-user via the existing manager/team RLS scoping (migration 012), this was exploitable today, not just a future risk. Fixed with a small `escapeHtml()` helper — deliberately duplicated inline in both content scripts rather than shared via import, since Rollup chunk-splits a module referenced by 2+ content-script entries and Chrome can't resolve an `import` statement in a classic (non-module) content script; verified empirically against the built `dist/assets/` output. `blockgate.js` was independently re-checked and confirmed clean (only interpolates `location.hostname`, which can't carry HTML-breaking characters). Canonical copy + 8 unit tests at `src/utils/escapeHtml.js` / `test/escapeHtml.test.js`.",
      "sections": []
    },
    {
      "version": "6.7.54",
      "title": "Session-aware device reclaim + session-id stamping (audit hardening)",
      "date": "2026-07-21",
      "intro": "> Hardens 6.7.53: a revoked device row now only un-revokes under a genuinely NEW auth session (GoTrue session_id comparison) - a surviving revoked session can no longer resurrect itself on the sync cycle. Every sync also stamps auth_session_id on this install's row, which makes remote sign-out actually able to revoke extension sessions (rows previously had no session id to revoke).",
      "sections": []
    },
    {
      "version": "6.7.53",
      "title": "Sign-in reclaims a revoked device row + clearer pairing-code auth error",
      "date": "2026-07-21",
      "intro": "> Parity with Sidecar 0.13.4: a fresh authenticated registration clears revoked_at on this install's own device row (a remote sign-out sweep otherwise left signed-in installs looking signed out). Pairing-code mint now says \"Session expired - sign out and back in\" instead of a raw \"unauthorized\" when the session was revoked.",
      "sections": []
    },
    {
      "version": "6.7.52",
      "title": "New logo (Split-Tab T) + pair-a-device code minting in the extension",
      "date": "2026-07-21",
      "intro": "> Extension icons switch to the new eye-free Split-Tab T mark. Settings -> Devices gains \"Pair a new device\": mint a one-time code (TV / Watch / Other, optional name) right from the extension - same pairing service the Sidecar and TV \"Sign in with a code\" flow use; previously minting was Sidecar-only.",
      "sections": []
    },
    {
      "version": "6.7.50",
      "title": "Device management in extension + home header layout fix",
      "date": "2026-07-21",
      "intro": "> Settings gains a Devices panel (list grouped per physical device, rename, pause/resume, remote sign-out, device-kind picker) backed by new background deviceService handlers; a never-blocking paused-device banner with one-tap Resume lands on home and sidebar (pause is a convenience flag, not a lock). Home header rebuilt: FlipClock contained (transform-scale reserved-box bug), greeting no longer truncates, right cluster spacing fixed, companion pill overlap eliminated. Merges feat/ext-device-management + fix/home-header-layout.",
      "sections": []
    },
    {
      "version": "6.7.47",
      "title": "Onboarding demo-walkthrough design spec",
      "date": "2026-07-20",
      "intro": "> Docs-only: approved design spec for the modular onboarding system (10 avatars, universal intro + branched journeys, tagged demo-data lifecycle, host-portable module registry, changelog-driven maintenance automation). No runtime changes.",
      "sections": []
    },
    {
      "version": "6.7.46",
      "title": "Device-row flood fix: adopt-before-insert",
      "date": "2026-07-20",
      "intro": "> browser_profiles registration now adopts this install/machine's newest existing row before ever inserting a fresh one, so an amnesiac install identity converges instead of minting duplicate device rows every wake (650-row flood root-caused on the desktop install). Diagnostics added for adoption + identity minting.",
      "sections": []
    },
    {
      "version": "6.7.45",
      "title": "Continuous cross-surface live ingest (focus + clock)",
      "date": "2026-07-20",
      "intro": "> Fixes the top-reported round-trip bug: intents/switches made in the Sidecar (or another browser install) never reached a running extension — it only pushed to Cloud Sync and rehydrated once per install. The extension now pulls continuously and arbitrates account-wide \"what's current\" for both focus and clock.",
      "sections": [
        {
          "label": "Added",
          "body": "- **Continuous focus ingest** — a 60s alarm (`focus-live-ingest`) plus a trigger right after every push cycle pull `focus_items` for the signed-in profile: a `created_at`-watermarked query for brand-new rows, plus a small `focus_state`+`tags`-only \"light sweep\" of every non-completed row to catch state changes (there's no `updated_at` column). Unknown rows are created locally via the same mapper the one-time rehydrate uses; known rows reconcile per a source-aware winner matrix — cloud wins for Sidecar-authored rows (`tags._src === 'sidecar'`), local wins for extension-authored rows.\n- **Account-wide focus arbitration** — the account's single \"current\" focus is whichever active row (any source) has the latest `tags._startedAt`. When a newer remote active row appears, the extension pauses its local current (normal pause path — events still emit) and adopts the newer one through a new suppressed activation path (`adoptRemoteActive`) that never re-emits a `start` event and never stamps a fresh timestamp, so the next push is a no-op and no ping-pong loop forms.\n- **Continuous clock ingest** — the same ingest cycle also pulls `browser_profile_status` rows (excluding self) and arbitrates the account's shift state by the latest `last_clock_event_at`, adopting a newer remote clock-in/break/clock-out into the local `clockSession` mirror (`clockService.applyRemoteClockState`) without touching `clockHistory` or re-pushing — the origin install's own history push remains the single source of truth for `clock_sessions`.\n- The extension now also mirrors `tags._startedAt`/`tags._elapsedMs` on every focus push (`syncService.buildFocusRows`), matching the Sidecar's own convention, so extension-authored rows are comparable on equal terms during arbitration from any surface.\n- `src/utils/liveIngestArbitration.js` — the pure comparator/reconcile-matrix logic, unit tested (30 tests, `test/liveIngestArbitration.test.js`) independent of chrome/supabase."
        }
      ]
    },
    {
      "version": "6.7.44",
      "title": "Sidecar 0.9-0.11 line reconciled",
      "date": "2026-07-20",
      "intro": "> Merge: invite signups (Demo/Personal/Team), TV code sign-in, Pomodoro mode, device management (naming/remote sign-out/pause), migrations 042-045, Olympus + Smart Deferral design docs (Koda-vetted). No extension code changed.",
      "sections": []
    },
    {
      "version": "6.7.43",
      "title": "Cross-surface round-trip parity",
      "date": "2026-07-20",
      "intro": "> Extension now emits focus_events (start/pause/resume/resolve/extend/backburner/snooze), syncs checkpoints to/from Cloud Sync, and renders Sidecar sub-intents nested + backburnered items grouped. Fixes backburner flag not clearing on direct switch.",
      "sections": []
    },
    {
      "version": "6.7.42",
      "title": "CWS publishing pipeline merged",
      "date": "2026-07-20",
      "intro": "> Tooling only: Chrome Web Store publish scripts + docs (feat/cws-activation). No extension code changed.",
      "sections": []
    },
    {
      "version": "6.7.41",
      "title": "Marketing site: /docs — screensaver (Flux Refocus) guide",
      "date": "2026-07-20",
      "intro": "> Marketing-site only (Cloudflare Pages `tabatha`, tabatha.pondocean.co). No extension code changed.",
      "sections": [
        {
          "label": "Added",
          "body": "- **`/docs/getting-started-screensaver.html`** — a 6th Getting Started card/guide: \"Screensaver (Flux Refocus),\" covering the Tabatha Context View mode that shipped 2026-07-19 in the separate `Flux-Refocus-Screensaver-clock` repo (v2.1.0, merge commit `0c36ad4`). Every label verified against that repo's `src/App.tsx` settings panel (`Show Tabatha Context View` toggle, `Sign in to Tabatha…` button) after a fresh `git pull`. Honest framing kept: it's a separate Electron app with no installer release yet, built from source and registered as a Windows screensaver via `install.ps1`.\n- Cross-links added: the Context View getting-started guide gets a 6th step (\"Put it on your screensaver (optional)\"); the Settings & customization guide's \"standing screen\" section now covers both Desk View and the screensaver.\n- `docs-search-index.json` regenerated (108 records, was 99).\n======="
        }
      ]
    },
    {
      "version": "6.7.41",
      "title": "Chrome Web Store API publishing pipeline reconciled into staging",
      "date": "2026-07-20",
      "intro": "> Merges `feat/cws-api` (built 2026-07-16 on `6.7.17`) forward onto the current staging line via `feat/cws-activation`.",
      "sections": [
        {
          "label": "Added",
          "body": "- **`scripts/cws-auth.mjs` + `npm run cws:auth`** — one-time OAuth bootstrap for the CWS Items API: auto-locates the downloaded \"Tabatha CWS Publisher\" `client_secret_*.json` in Downloads, runs a local loopback consent flow (opens the default browser, no code-pasting), and writes `CWS_CLIENT_ID` / `CWS_CLIENT_SECRET` / `CWS_REFRESH_TOKEN` into the gitignored `deploy-creds.local`.\n- **`scripts/cws-publish.mjs` + `npm run cws:upload` / `cws:publish` / `cws:status`** — the release pipeline: mints an access token from the stored refresh token, uploads `store-assets/tabatha-store-v<version>.zip` (building it via `build:store` first if missing) to a new (`--new`, writes `CWS_APP_ID`) or existing item, publishes via `publishTarget` (defaults to `trustedTesters` for the staff rollout; `--target default` for the wider unlisted audience), and reports draft `--status`. Surfaces the API's own `itemError` messages verbatim; never logs a token value.\n- **`scripts/lib/deploy-creds.mjs`, `scripts/lib/cws-client.mjs`, `scripts/lib/cws-args.mjs`, `scripts/lib/cws-zip.mjs`** — pure, unit-tested helpers backing both scripts.\n- **`docs/cws-api-release.md`** — the runbook: one-time auth, the per-release loop, `publishTarget` vs. Visibility, review-latency expectations.\n- **`docs/CWS-PUBLISHING.md`** — the one-command flow plus the one-time human setup steps (account registration/fee, OAuth consent, first-listing creation as Private/domain-only, Workspace force-install repoint)."
        },
        {
          "label": "Fixed",
          "body": "- **Replaced the orphaned, unwired `scripts/cws-auth.mjs`** that had landed on staging via an unrelated mobile-responsiveness merge (`8aa2d0b`, sourced from `feat/showcase-responsive`). That copy used Google's deprecated `urn:ietf:wg:oauth:2.0:oob` out-of-band flow and was never wired into `package.json` or paired with a publish script. This branch's loopback-listener implementation supersedes it.\n======="
        }
      ]
    },
    {
      "version": "6.7.41",
      "title": "Extension ↔ Sidecar round-trip parity v1",
      "date": "2026-07-20",
      "intro": "> Closes the biggest cross-surface gap flagged on the Feature Matrix: the extension could not see or produce what the Tabby Sidecar (mobile companion) now tracks.",
      "sections": [
        {
          "label": "Added",
          "body": "- **`focus_events` emission**: the extension now writes best-effort `tabatha.focus_events` rows (`source:'extension'`) at every start/pause/resume/resolve/extend/snooze/backburner/unbackburner transition, using the same kinds and meta shapes as the Sidecar (`sidecar/src/data/events.ts`). Events are appended locally (`_focusEventLog`) and batched to the cloud on the existing sync cadence, mirroring the `intent_history` push pattern — a write failure can never block the underlying focus action.\n- **`focus_checkpoints` round-trip**: the sidebar's existing checkpoint composer (`SAVE_CHECKPOINT_NOTE`) now reaches the cloud — user-authored notes are pushed to `tabatha.focus_checkpoints`, and cloud-authored notes (e.g. from the phone) are pulled back into the local checkpoint timeline, both on the regular sync cycle. System-generated auto-checkpoints stay local (now covered distinctly by `focus_events`).\n- **Sidebar sub-intent / backburner rendering**: the sidebar queue now nests sub-intents of the current focus (↳ badge + indent) and moves backburnered items into their own collapsed dock instead of also showing them in the main queue. Reads both the extension's dedicated fields (`parentFocusId`/`backburnered`) and the Sidecar's tag convention (`tags._parent`/`tags._backburner`) so items from either surface render correctly."
        },
        {
          "label": "Fixed",
          "body": "- **`switchFocus` never cleared `backburnered`**: switching directly into a backburnered item left it stuck marked as backburnered even while active. Now cleared on activation (mirrors the Sidecar's `switchTo`)."
        },
        {
          "label": "Changed",
          "body": "- **`buildFocusRows` (sync push)**: now mirrors `item.parentFocusId`/`item.backburnered` into `tags._parent`/`tags._backburner` on every push, so extension-created sub-intents/backburner state render correctly on the Sidecar and Context View.\n- **`serverFocusToLocal` (rehydrate pull)**: now mirrors `tags._parent`/`tags._backburner` into `parentFocusId`/`backburnered` when present, without clobbering local-only values when absent."
        }
      ]
    },
    {
      "version": "6.7.40",
      "title": "Marketing site: new /docs help section",
      "date": "2026-07-20",
      "intro": "> Marketing-site only (Cloudflare Pages `tabatha`, tabatha.pondocean.co). No extension code changed.",
      "sections": [
        {
          "label": "Added",
          "body": "- **`/docs`** — a new task-oriented help section on the public site, alongside the existing `/show` component showcase. Landing page with its own search box (separate `docs-search-index.json` so `/show`'s card count stays untouched) laid out on two axes: **Getting Started** (one guide per surface — Extension, Tabby Sidecar, Context View, Desktop Companion, Tabby Watch) and **How do I…** (9 task categories — Focus & intents; Timers, extensions & backburner; Checkpoints & voice; Tasks & Asana sync; Work shifts & clock; Phone Focus Mode & the red screen; Pairing devices; Settings & customization; Feedback & troubleshooting). 14 guide pages, every step verified against the current `src/`, `sidecar/src/`, `tabatha-watch`, and `tabatha-desktop` source rather than paraphrased from memory. Honest framing kept for beta bits (Tabby Watch is a sideload beta; the Desktop Companion installer is unsigned and triggers SmartScreen).\n- **\"Docs\" nav link** added to the homepage footer and to every `/show` page's header nav (index, roadmap, and all 10 component-category pages), matching the existing sticky-nav/mobile-drawer mechanics. `/docs` pages cross-link back to the relevant `/show` surface and component pages (\"See it: …\")."
        }
      ]
    },
    {
      "version": "6.7.39",
      "title": "Sidecar line reconciled into staging",
      "date": "2026-07-19",
      "intro": "> Merge commit: brings the entire Tabby Sidecar 0.x line (sidecar/ app v0.8.2, Supabase migrations 030-041 + 5 edge functions, Plan 040/041 specs and docs) into staging. No extension code changed.",
      "sections": []
    },
    {
      "version": "6.7.38",
      "title": "Feature intake: tab grouping, agent browsing, agent history specs",
      "date": "2026-07-19",
      "intro": "> Docs-only. No extension code changed.",
      "sections": [
        {
          "label": "Added",
          "body": "- **Feature spec #217 — Intent Tab Grouping Suite** (`docs/features/217-intent-tab-grouping-suite.md`): per-intent tab counts (related/open/closed) on home + sidebar, \"group this intent's tabs\" action, multiple tab groups per intent across windows (multi-monitor use case), multi-select tabs → toolbar icon contextual group/split menu, consolidate-into-N-windows gather action, and foreign-group adoption. Phased 1–3; Phase 3's cross-window live reparenting question is explicitly left open.\n- **Feature spec #218 — Agent Browsing Detection** (`docs/features/218-agent-browsing-detection.md`): classify agent-created tab groups via the C11a agent-session service plus honest heuristics, suppress InPop or show an InPop-Agent variant (no Sugar Box / Side Quest for agents), manual per-group override.\n- **Feature spec #219 — Agent vs Human History** (`docs/features/219-agent-vs-human-history.md`): actor attribution (`human` | `agent:{sessionId}`) on every history-bearing record, LogsPanel filter chips, agent time excluded from analytics by default, per-session agent audit view."
        },
        {
          "label": "Changed",
          "body": "- **#211 Audio Input & Voice Control**: InBar mic button promoted to a first-class requirement (speak notes, checkpoints, intent changes); `log_checkpoint` / `append_note` / `change_intent` added to the voice action-routing schema; C9 voice v0 groundwork referenced.\n- **features.md matrix**: registered #215–#219 under a new \"Input, Sessions, Grouping & Agent Browsing (Backlog 2026-07-16)\" section (215/216 had specs but were never registered)."
        }
      ]
    },
    {
      "version": "6.7.37",
      "title": "Marketing site: Desktop Companion 0.3.1 download live",
      "date": "2026-07-19",
      "intro": "> Marketing-site only (Cloudflare Pages `tabatha`, tabatha.pondocean.co). No extension code changed.",
      "sections": [
        {
          "label": "Changed",
          "body": "- **Companion download panel lifted from the deliberate 0.2.0 pin to 0.3.1.** The `/show` companion page's download panel now serves the 0.3.1 installers: NSIS `Tabatha.Desktop_0.3.1_x64-setup.exe` (4,927,360 bytes, SHA-256 `99828808694b6ff8606975c1a52354f61b18d66f78e82f1b70f1378e617fc6a7`) and `.msi` (8,175,616 bytes, SHA-256 `9969d5dc255294ade3ef1e42db8abd18763eec50c7fb30be422032231a6b384a`), with a note that this build ships the hardened one-tap device pairing and the unsigned-binary/SmartScreen caveat kept. Version badges on the companion page (h1, tray-tooltip and Desk Panel mockups, footer) and the `/show` family tile + get-strip updated to 0.3.1.\n- **Release mirrored to the public repo.** The 0.3.1 artifacts were released on the private `tabatha-desktop` repo, which the public cannot download from; following the `desktop-v0.2.0` precedent, both installers were mirrored (hash-verified before upload) to a public `desktop-v0.3.1` release on the main repo, and the panel links point there so every link works anonymously."
        }
      ]
    },
    {
      "version": "6.7.36",
      "title": "Marketing site: Tabby Sidecar & Tabby Watch full-page presence",
      "date": "2026-07-18",
      "intro": "> Marketing-site only (Cloudflare Pages `tabatha`, tabatha.pondocean.co). No extension code changed.",
      "sections": [
        {
          "label": "Added",
          "body": "- **Tabby Sidecar gets a full detail page.** New `site/show/components-sidecar.html` (12 components, rendered from the app's real tokens — `sidecar/src/lib/theme.ts`, accent `#00E0D6`): sign-in, active focus & queue, voice capture and proactive check-ins, Tasks & Asana sync, Context View (including the phone-away red overlay), Phone Focus Mode, PWA install & self-healing, Web Push, in-app feedback, Notes/simple capture, watch pairing, and Cloud Sync. The `/show` family tile now links to this page instead of straight out to the live app; \"Open Tabby Sidecar\" remains as its own strip. Sidecar reflected at v0.7.2.\n- **Tabby Watch joins the showcase family.** New `site/show/components-watch.html` (8 components, rendered from `tabatha-watch`'s Wear OS Compose theme — green/amber/red focus-ring states): the focus ring, action chips, checkpoint quick-add, shift glance, Tile, complication, password-free 6-digit pairing, and the architecture/fixture-mode card. Framed honestly throughout as a **sideload beta** — no store listing, built and sideloaded from `MrMalkio/tabatha-watch` (v0.1.2). A new family tile and a non-`.ready` get-strip point here; the family section is now \"4 in the family\".\n- **Roadmap.** Replaced the Tabby Watch Todo card with a Resolved card (v0.1.2, sideload beta) and added a new Todo card for the store release that remains outstanding.\n- **Search index & site build.** `scripts/build-search-index.mjs` now indexes both new pages (140 component cards total, up from 120); ran `npm run site:build` to regenerate `search-index.json` and `privacy.html`."
        }
      ]
    },
    {
      "version": "6.7.35",
      "title": "Marketing site: Tabby Sidecar v0.7.0 + Extension 6.7.34 milestone",
      "date": "2026-07-18",
      "intro": "> Marketing-site only (Cloudflare Pages `tabatha`, tabatha.pondocean.co). No extension code changed.",
      "sections": [
        {
          "label": "Added",
          "body": "- **Tabby Sidecar v0.7.0 reflected on the showcase.** The `/show` family tile and \"Open Tabby Sidecar\" strip now cover the Tasks view with Asana personal-access-token sync (subtasks, blocked badges, tracked time, start-task-into-focus), timer extensions and snoozes as ⏳ nodes on the Context View timeline, proactive voice check-ins (Tabatha asks how it's going out loud, applies your spoken answer with a visible Undo, opt-in and quiet-hours aware), and the self-healing PWA (auto-update + crash recovery screen).\n- **Extension 6.7.34 reflected on the showcase.** The Browser Extension family tile now mentions Context View customization from Settings (day countdown, up-next, timeline, checkpoint preview, phone-away fade, day-reset hour — synced with the phone) and the Desktop Companion pairing card. The Desktop Companion tile and detail page describe the new hardened, one-tap pairing token exchange (Desktop Companion 0.3.1) in place of the earlier open local socket.\n- **Roadmap.** Added Resolved cards for the Sidecar Tasks/Asana sync, timer-extension timeline nodes, self-healing PWA, proactive voice check-ins, extension Context View customization, and hardened companion pairing. Added a Todo card for Tabby Watch (Wear OS)."
        }
      ]
    },
    {
      "version": "6.7.34",
      "title": "Changelog backfill for 6.7.32/6.7.33 - docs only",
      "date": "2026-07-18",
      "intro": "> Documentation-only version bump (version-per-commit rule).",
      "sections": []
    },
    {
      "version": "6.7.33",
      "title": "Companion pairing: WS Stage-2 HELLO client",
      "date": "2026-07-18",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Companion HELLO handshake (client side).** The background's companion bridge now sends `HELLO` (pairing token + surface/profile hint) as its first WebSocket message, matching Desktop Companion 0.3.1's Stage-2 auth. Handles `HELLO_ACK` (post-open sync fires after ack, or after a 2s fallback for 0.3.0 companions) and `HELLO_REJECTED` (60s reconnect backoff + \"pairing required\" banner).\n- **Pairing card** in Settings → Desktop Activity: paste the token from the companion tray's \"Pair Extension\" action (password field; token stored in extension storage only, never logged)."
        }
      ]
    },
    {
      "version": "6.7.32",
      "title": "Context View customization from the extension",
      "date": "2026-07-18",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **📺 Context View settings section** (Epic 9): toggles for day countdown, up-next, bottom timeline, checkpoint counter/last-note preview, phone-away fade mode (slow/immediate), and day-reset hour. Writes go through the new atomic `update_profile_settings` Cloud Sync RPC (migration 038), shared with Tabby Sidecar — no more cross-surface settings clobbering."
        }
      ]
    },
    {
      "version": "6.7.31",
      "title": "Marketing site: Tabby Sidecar v0.4.0 milestone",
      "date": "2026-07-18",
      "intro": "> Marketing-site only (Cloudflare Pages `tabatha`, tabatha.pondocean.co). No extension code changed.",
      "sections": [
        {
          "label": "Added",
          "body": "- **Tabby Sidecar v0.4.0 reflected on the showcase.** The `/show` family tile and \"Open Tabby Sidecar\" strip now cover voice capture (speak an intent or a checkpoint), the Chaperone v0 personality interrupt (\"Don't you dare touch that phone\"), Context View's live bottom timeline (checkpoints, starts, overtime pulse on a reworked layout), and pause-on-leave Phone Focus Mode.\n- **Roadmap.** Moved \"Voice input\" from Todo to Resolved (v0.4.0), and added Resolved cards for the Sidecar Chaperone and the Context View bottom timeline. The native iOS/Android app stays a Todo card."
        }
      ]
    },
    {
      "version": "6.7.30",
      "title": "Widget carve-out: docs only",
      "date": "2026-07-18",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **`docs/WIDGET-VERSIONING.md`** — proposal (not executed) for splitting the Asana widget onto its own `0.x` version line, separate from the extension's `6.x` line, per Plan 040 Addendum 4. Written as part of untangling `feat/companion-update-manifest`/`feat/site-sidecar-promo` from `Koda/asana-widget-pre-rebase`; no widget code, version files, or branches were touched."
        }
      ]
    },
    {
      "version": "6.7.29",
      "title": "Marketing site: Tabby Sidecar promotion",
      "date": "2026-07-18",
      "intro": "> Marketing-site only (Cloudflare Pages `tabatha`, tabatha.pondocean.co). No extension code changed.",
      "sections": [
        {
          "label": "Added",
          "body": "- **Sidecar entry point on the teaser homepage.** A top-right \"Sign in\" control links to the live Tabby Sidecar mobile companion at `/sidecar`, styled in the teaser's own token system.\n- **Tabby Sidecar on the showcase.** The `/show` family section now presents Tabby Sidecar as a shipped mobile web companion (sign in, queue and active focus, create intent, run the clock, Web Push, PWA install), with an \"Open Tabby Sidecar\" strip. The native iOS/Android app is reframed as the next step.\n- **Roadmap.** Added a Resolved card for Tabby Sidecar (mobile web companion) and a Todo card for the native iOS and Android app."
        }
      ]
    },
    {
      "version": "6.7.28",
      "title": "Desktop companion updater manifest",
      "date": "2026-07-18",
      "intro": "> Marketing site only (Cloudflare Pages `tabatha`, tabatha.pondocean.co). No extension code changed.",
      "sections": [
        {
          "label": "Added",
          "body": "- **Desktop companion self-update manifest.** Serves the Tauri v2 updater manifest at `/desktop/latest.json` so the companion can self-update. Points `windows-x86_64` at the `desktop-v0.2.1` GitHub release NSIS installer with its minisign signature."
        }
      ]
    },
    {
      "version": "6.7.27",
      "title": "Changelog drift gate no longer trips on Windows line endings",
      "date": "2026-07-18",
      "intro": "",
      "sections": [
        {
          "label": "Fixed",
          "body": "- **`build-changelog --check` failed on every fresh Windows checkout** (`core.autocrlf=true` smudges `public/changelog.json` to CRLF while the generator emits LF, so the byte compare always mismatched). The check now normalizes CRLF before comparing — purely content-driven, as intended."
        }
      ]
    },
    {
      "version": "6.7.26",
      "title": "Changelog JSON regenerated after restoration merges",
      "date": "2026-07-18",
      "intro": "",
      "sections": [
        {
          "label": "Fixed",
          "body": "- **`public/changelog.json` was stale vs `Tabatha_Changelog.md`**, tripping the prebuild `build-changelog --check` gate. The GitHub-restoration merges (PRs #28/#29) combined changelog entries from the extension and sidecar lines without regenerating the JSON. Regenerated (65 releases)."
        }
      ]
    },
    {
      "version": "6.7.25",
      "title": "Reach the empty focus slot: no auto-start on resolve + home shows the queue",
      "date": "2026-07-18",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **\"Auto-start next intent on resolve\" toggle** (Settings → Focus Lifecycle, default ON — existing behaviour unchanged unless you opt out). Resolving an intent has always pulled the most-recently-paused intent out of the queue and straight into focus. Turn this OFF and resolving leaves **nothing** active: the queue stays paused until you pick the next intent yourself. This also makes an empty focus slot reachable for the first time — pausing never clears `activeFocusId` (it only flips `focusState` to `paused`), so `completeFocus` was the only path that emptied the slot, and it immediately re-filled it."
        },
        {
          "label": "Fixed",
          "body": "- **The focus queue, backburner, and history no longer vanish from the home page when nothing is active.** They were nested inside the `activeFocus ? … : …` branch, so an empty focus slot rendered only the \"What are you focusing on?\" input — the queued intents were invisible on the home page (the sidebar showed them; home didn't). They now render regardless of active state, pairing with the toggle above."
        }
      ]
    },
    {
      "version": "6.7.24",
      "title": "Staff auto-update swap step fixed",
      "date": "2026-07-17",
      "intro": "",
      "sections": [
        {
          "label": "Fixed",
          "body": "- **Staff auto-update client silently failed every swap** (`scripts/tabatha-updater.ps1`). Root cause: the script (and `scripts/install-tabatha-staff.ps1`, same pattern) was saved as UTF-8 **without a byte-order mark**, and contains em-dash characters inside executable `Log` string literals. Windows PowerShell 5.1 (`powershell.exe`, the interpreter staff machines actually run) decodes BOM-less `.ps1` files using the system ANSI codepage instead of UTF-8 — the em-dash's multi-byte UTF-8 sequence gets misdecoded, which corrupted the tokenizer's parse of the surrounding statements and silently skipped the `Expand-Archive` extraction call and the `Test-BuildValid` validation gate entirely. Execution fell through straight to reading a version from a directory that was never extracted, producing the empty-version log line and a swap failure (\"Cannot find path '...\\extracted'\"). Fixed by adding a UTF-8 BOM to both scripts (the encoding Windows PowerShell 5.1 requires to correctly read non-ASCII bytes). Also added an explicit post-extraction existence check in the updater so any future extraction failure fails loudly at validation instead of surfacing as a confusing error at swap time. Verified end-to-end against the live channel: stable path went v6.7.19 -> v6.7.23, and a second run correctly no-ops (idempotent, no orphaned staging/old dirs). `mirror-extension.ps1` is plain ASCII and does not share this defect."
        }
      ]
    },
    {
      "version": "6.7.23",
      "title": "Update-channel test + re-sync",
      "date": "2026-07-17",
      "intro": "",
      "sections": [
        {
          "label": "Fixed",
          "body": "- **Self-hosted update channel had drifted to v6.7.15** — `publish:update` was not run for several releases, so remote staff installs would not have auto-updated past 6.7.15. This release re-publishes the channel and was used to verify the end-to-end auto-update path (publish -> latest.json -> client pull -> stable-path swap) works."
        }
      ]
    },
    {
      "version": "6.7.22",
      "title": "Desktop companion shipped as a real Windows download",
      "date": "2026-07-16",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **The desktop companion is now downloadable.** It was built and running at v0.2.0 but\n  existed only as a bare `.exe` on the machine that compiled it — no installer, no host.\n  Ran the Tauri bundler to produce a real **NSIS installer** (`4,666,565` bytes, sha256\n  `674ec83c…d8846`) plus an MSI, and published both as a **GitHub release** on\n  `MrMalkio/tabatha` under the tag **`desktop-v0.2.0`** — deliberately distinct from the\n  extension's `ext-v*` update channel so the two release lines cannot collide. The source\n  repo is separate and unpublished; only the built artifact is hosted.\n- **The showcase companion card now offers the real file.** `site/show/components-companion.html`\n  and the `site/show/index.html` entry strip replace the honest \"request access\" soft state\n  with a genuine download card: version 0.2.0, Windows x64, 4.45 MB, the full SHA-256, a\n  direct release-asset link, and a `.msi` alternative. The CTA is promoted from amber\n  (\"honest, but not yet\") to cyan (\"go\"). Verified responsive with no horizontal overflow\n  at 1280/768/480; the sha256 wraps within bounds on mobile."
        },
        {
          "label": "Fixed",
          "body": "- **Companion README corrected.** `tabatha-desktop/README.md` claimed the companion takes\n  **\"no screenshots, no keyloggers, no content capture\"** and was stamped v0.1.0. A GDI\n  screen-capture engine ships as of v0.2.0 (`screen_capture.rs`, merged to master), so the\n  screenshot claim was false. The README now describes capture honestly under the same\n  terms as the privacy policy — opt-in, off by default, redacted fail-closed, written only\n  to the user's own machine, never uploaded, auto-pruned 30d personal / 90d org-clocked —\n  fixes the stale version references, and documents the local-socket limitation."
        },
        {
          "label": "Security (disclosed, not yet fixed)",
          "body": "- **Documented the companion's unauthenticated local socket.** The companion's WebSocket on\n  `127.0.0.1:9147` (loopback-only, never network-exposed) does not authenticate local\n  callers. Because WebSocket connections bypass the same-origin policy, **any web page you\n  visit** can connect and — most seriously — silently enable OS screen capture via\n  `CAPTURE_CONFIG`. Full assessment and a staged, non-breaking fix in\n  `docs/security/companion-socket-auth.md`; surfaced publicly on the companion card as\n  \"local-only interface, authentication hardening in progress\" (no exploit recipe). The fix\n  is **not** shipped here — it must land on both the companion and the extension bridge\n  together to avoid breaking the existing connection."
        }
      ]
    },
    {
      "version": "6.7.21",
      "title": "Privacy policy correction + teaser H1 matches shipped Gatekeeper",
      "date": "2026-07-16",
      "intro": "",
      "sections": [
        {
          "label": "Fixed",
          "body": "- **`PRIVACY.md` was making false claims and has been corrected.** The published policy\n  (live at `/privacy`, and the privacy URL the Chrome Web Store listing points at) stated\n  that Tabatha collects **\"No screenshots of your pages\"** and knows \"never *what you did\n  on the page*\". That has been untrue since the Cortex capture engine shipped: the\n  extension runs `chrome.tabs.captureVisibleTab` on a dwell timer, and the desktop\n  companion captures at the OS level. This was a real disclosure defect, not a wording\n  nit — a privacy policy that under-describes collection is the worst document in this\n  repo to get wrong, and a false disclosure checked against declared behavior is a Chrome\n  Web Store rejection risk on top of the trust problem.\n  The policy now states the truth, which is a defensible story rather than a silent one:\n  capture is **opt-in and off by default** (`screenshotCapture: false`,\n  `src/background/constants.js:51`), frames are **redacted with a fail-closed pass**,\n  written **only to the user's own machine** (companion folder, OPFS fallback), **never\n  uploaded** (no upload path exists — frames appear in neither `syncService.js` nor any\n  AI routing tier), and **auto-pruned** at 30 days personal / 90 days org-clocked.\n  A dated **\"What changed\"** section was added to the policy itself so the revision is\n  visible to readers rather than being a quiet edit of a published page.\n- **New policy sections** covering the desktop companion's OS-level capture (same terms),\n  AI features (default tier is `harness` = local file only, sends nothing; `proxy` /\n  `gateway` / `byok` are text-only by design, not currently selectable, and require\n  sign-in or user-supplied provider details), and organization accounts — which state\n  accurately that org capture policy (`org_capture_policy`, migration 023) is **schema\n  with no client consumer**, so no admin can force capture on today.\n- **Downstream copies of the false claim synced** — `docs/CHROME-WEB-STORE-LISTING.md` §7\n  (the text pasted into the store's privacy-practices form, now with an explicit\n  data-usage-form note) and `TEAM-ONBOARDING.md` (which the listing had been quoting as\n  its source). `site/privacy.html` regenerated from `PRIVACY.md` via\n  `scripts/build-privacy.mjs`.\n- **Teaser H1 copy drift closed.** `site/index.html` led with *\"What are you down for?\"*,\n  but the shipped Gatekeeper overlay asks **\"Why are you here?\"** (`src/content/gatekeeper.js:288`).\n  The teaser now asks the real question; the live elapsed-time instrument and its\n  \"Unaccounted for\" caption are unchanged and land harder against it. The same drift is\n  fixed in `docs/CHROME-WEB-STORE-LISTING.md` §3 and its permission justifications, where\n  a description promising a prompt the extension does not show would be flagged on review."
        },
        {
          "label": "Changed",
          "body": "- **Agent-Ready Data pillar** now closes on what the data is *for*, not just what it is —\n  agents \"help you do better work with surgical accuracy\". Applied in each surface's own\n  voice: `Tabatha_Concept.md` §6, `site/index.html` pillar, `site/show/index.html` pillar\n  (whose old sub-blurb, *\"structured, not screenshots\"*, was itself part of the false\n  claim), and the store listing's AGENT-READY DATA block."
        }
      ]
    },
    {
      "version": "6.7.20",
      "title": "The public site works on a phone",
      "date": "2026-07-16",
      "intro": "",
      "sections": [
        {
          "label": "Fixed",
          "body": "- **The site was not responsive at any width below desktop.** Measured before the fix: **33 of 54** page/width combinations overflowed horizontally. Every one of the 18 pages failed at 390px, and 7 of 10 still failed at 768px. Now 54/54 pass. Breakpoints are **1024 / 768 / 480**, and the rule is that the page body never scrolls sideways: anything intrinsically wide (component stages, the log table, the roadmap rail) scrolls inside its own container instead.\n- **The `.reqbtn` header button alone broke every page.** \"✨ Request a feature\" with `white-space: nowrap` was a flex item that could not shrink, so it set a 427px floor under the nav and therefore under the whole document, on all 10 site pages. Its label now lives in a `<span class=\"lbl\">` and collapses to the glyph below 768px, with the name kept on `aria-label`.\n- **Four surface frames shipped no `<meta name=\"viewport\">`** (`sidebar`, `home`, `settings`, `backdating`), so a phone laid them out against a 980px virtual viewport. Added.\n- **Per-card actions were unreachable on touch.** `.cardfb` and the roadmap's `.rfb` are revealed on `:hover`, which a touch device never fires, so Bug/Request could not be tapped at all. Now permanently visible under `@media (hover: none)`. Desktop capture is unaffected.\n- **Component stages clipped their contents on a phone.** `.stage` is `overflow: hidden` and the demos inside are pinned-width renders of real app UI (the CommandPalette is 540px because the CommandPalette *is* 540px), so a phone silently cut them in half. The stage now scrolls on its own axis below 900px, start-aligned so the overflowing edge is actually reachable."
        },
        {
          "label": "Added",
          "body": "- **Mobile nav drawer.** `site.js` wraps the header links in `.navlinks` and injects a `.navtoggle`; above 768px the wrapper is `display: contents`, so the desktop nav renders exactly as before. Done in JS rather than in markup because the ten pages carry the same nav with different link sets.\n- **Full-height sheet for the search and feedback overlays** below 768px, with the field pinned at the top and the list taking the remaining height. Inputs are 16px to stop iOS zooming the viewport on focus.\n- **Companion download panel** (`showcase/components-companion.html#get`) plus an entry strip on the hub. It is deliberately a **soft state**: there is no artifact to link. It names the platform (Windows 10/11 x64), the real version (**0.2.0**, from `tauri.conf.json`, not the stale README), what the companion actually does *including that it captures the screen*, and an explicit list of what blocks a release (no git remote, no `tauri build` bundle, no code signing, no companion update channel, stale docs, unauthenticated local socket). The CTA files a request rather than pretending to serve a file."
        },
        {
          "label": "Changed",
          "body": "- **Hub thumbnails now derive their scale from their own box** (`container-type: inline-size` + `aspect-ratio` + `scale(calc(100cqw / 1280px))`) instead of hard-coded transform/height pairs that only agreed at one viewport. The phone step previously cropped roughly half of every surface.\n- **Roadmap board has three shapes**: six columns at desktop, a snap-scrolling rail from 769-1024px, and stacked below 768px. A 232px column on a 390px screen turns the roadmap into six swipes; stacked, it reads top to bottom.\n- **Surface frames scale rather than reflow.** All 8 stay exactly 1280×800 at the capture width; below 1279px `zoom: calc(100vw / 1280px)` fits the whole frame to the viewport, with body-level scrolling as the fallback where that is unsupported. `.shot`'s own layout is never touched, so the Chrome Web Store screenshots are byte-identical.\n- **Companion status reads \"Built · v0.2.0\", not \"Shipped\".** The page previously claimed \"Everything on this page is shipped and installable today\"; nothing on it is installable, because the binary has never left the machine that compiled it."
        }
      ]
    },
    {
      "version": "6.7.19",
      "title": "Teaser homepage, waitlist, showcase moves to /show",
      "date": "2026-07-16",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Teaser homepage** (`site/index.html`) — the site root is now a held moment rather than a component gallery. It asks the Gatekeeper's own question (\"What are you down for?\") and answers it with a live instrument counting the visitor's real elapsed time on the page, captioned \"Unaccounted for.\" One idea, one input, no feature dump. Tokens are lifted verbatim from the showcase's `:root` (2/4/8 radius, `#050505`, `#00F0FF`, the Inter / JetBrains Mono stacks) so the teaser and the product read as one system. Self-contained: no CDN, no external font, no analytics, no beacon.\n- **Waitlist** (`functions/api/waitlist.js` + `supabase/migrations/028_waitlist.sql`) — the page's only CTA and only real action. Email posts to a Pages Function holding the service-role key in an env binding; **no Supabase key is ever shipped to the browser.** Server-side validation, per-isolate rate limiting, and duplicates treated as success via `ON CONFLICT DO NOTHING` so the endpoint can never be used to test whether an address is on the list. Returns 501 when unconfigured and the page says so plainly rather than faking a signup.\n- **Privacy page** (`site/privacy.html`, generated) — `/privacy` is now a real page, rendered from `PRIVACY.md` by `scripts/build-privacy.mjs` so the published policy cannot drift from the repo's. Linked from the teaser footer and from the signup fine print, because the site now collects email addresses.\n- **Waitlist section in `PRIVACY.md`** — covers what the address is used for (one email, once), that it is unlinked to any browsing data, that the list is not publicly readable, and how to be removed. The policy's scope line now names the website as well as the extension."
        },
        {
          "label": "Changed",
          "body": "- **`showcase/` is now `site/show/`, and `site/` is the deploy root.** Done as pure renames, so every one of the 24 files retains its history. No page content changed: every internal link and both data fetches were already relative, so `/show/` works exactly as `/` did. Deploy is now `wrangler pages deploy site`.\n- **`_headers` moved to the deploy root** (`site/_headers`) and its data/asset rules repathed under `/show/`. Pages only reads `_headers` at the root of the deployed directory, so left in place it would have silently stopped applying: the CSP, the frame rules and the cache policy would all have quietly vanished.\n- **`scripts/serve-showcase.mjs` now serves the whole deploy root**, resolving directory requests and clean URLs the way Pages does, so `/`, `/show/` and `/privacy` behave locally exactly as deployed. `/api/waitlist` is stubbed at 501 alongside `/api/feedback`.\n- **`scripts/build-search-index.mjs` and `scripts/capture-screenshots.mjs`** read from `site/show/`. The five pinned Chrome Web Store screenshots are untouched: same filenames, same `store-assets/screenshots/` destination, same enforced 1280x800."
        },
        {
          "label": "Security",
          "body": "- **`tabatha.waitlist` is deny-by-default**: RLS enabled and forced, with **zero policies**. A waitlist signup is anonymous by definition, so there is no identity to scope a policy to and none is written; `service_role` bypasses RLS and is the single intended path in. The inherited `anon` / `authenticated` grants from migration 006's `ALTER DEFAULT PRIVILEGES` are explicitly revoked, so both locks must fail before an address could leak."
        }
      ]
    },
    {
      "version": "6.7.18",
      "title": "Public site covers the whole product family",
      "date": "2026-07-16",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Desktop Companion category page** (`showcase/components-companion.html`) — 15 cards covering the shipped **v0.2.0** companion: tray menu and its runtime-rasterized icon, Desk Panel, InstallGuide (connected · disconnected · never-installed), window monitor, time clock, today summary, recent activity, categorizer, WebSocket bridge (all 10 outbound + 12 inbound message types), the SQLite activity log, opt-in screen capture, extension updater, autostart + `tabatha://`, and the tray's text activity report. Stages render the companion's **own** tokens (`#4a9eff` / `#10ac84`, 8/4 radius), because it is a different app from the extension.\n- **Mobile App category page** (`showcase/components-mobile.html`) — 15 cards, **every one tagged \"In development\"**: nothing here is installable. 7 are marked *Built in app* (Dashboard, Settings, bottom-tab nav, persistent notification, native usage monitor, categorizer, hybrid LAN-first sync) and 8 *Planned* (Focus Hub, activity timeline, analytics, tasks, shift controls, InBar floating widget, onboarding, Morning Kickstart), taken from the canonical repo's `FEATURES.md` checkboxes and verified against `src/`.\n- **Product status badge system** (`showcase/assets/site.css`) — `.statusflag` (`live` / `dev` / `planned`) reusing the app's own semantic colors, a per-card `.substatus`, and a page-level `.devnote`. Same pill geometry as the hub's existing `.cwsflag`, so it reads as one vocabulary.\n- **\"The family\" hub section** — the three products, what each is for, and why they are separate: an extension sees tabs but not windows, a desktop app sees windows but not what is inside a tab, and neither is in your pocket. One shift at a time, wherever you started it.\n- **\"Why?\" on feature requests** — its own labelled textarea, distinct from the description, asked for once and yielded on rather than hard-blocked. Carried into both the Asana task body (as its own `WHY THIS MATTERS` block above the metadata rule, with an explicit `(not supplied)` when skipped) and the prefilled GitHub-issue fallback (as a `### Why this matters` section). Bug reports keep exactly the fields they had."
        },
        {
          "label": "Fixed",
          "body": "- **TagPicker was missing its personal-realm states** — the card showed only the business realm. Added *expanded / personal selected* (client auto-filled to **Self**, Project dropped as a business-only field, Task retained) and a *collapsed / personal* breadcrumb, matching `src/components/ui/TagPicker.jsx`.\n- **Search index folded badge text into record names** — `.cwsflag` and the new `.statusflag` sit inside the `.t` caption, so the extractor produced records literally named `\"The Gatekeeper Store shot\"`, and would have produced `\"Dashboard In development\"` for all 25 new cards. Badges are now stripped from the name and folded into `keywords`, where \"in development\" is genuinely worth matching."
        },
        {
          "label": "Changed",
          "body": "- **Search index: 147 → 185 records** (120 component cards across 8 category pages, 11 pages, 12 sections, 8 surfaces, 34 roadmap items). `scripts/build-search-index.mjs` gained the two new pages, an `EXPECTED_CARDS` constant, and family-card indexing; `scripts/capture-screenshots.mjs` gained the two new card pages. The 5 Chrome Web Store shots are untouched and still validate at exactly 1280×800."
        }
      ]
    },
    {
      "version": "6.7.17",
      "title": "Showcase becomes a public site: purpose copy, search, feedback, roadmap",
      "date": "2026-07-16",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Site-wide search** — one box in the header of every real page (hub, all 6 category pages, roadmap), querying a prebuilt static index of **147 records**: all 90 component cards, 9 section descriptions, 8 surfaces, 6 pages, and every roadmap item. Fully keyboard-driven: `/` focuses from anywhere, arrows move, Enter opens, Escape closes. `npm run build:search-index` generates `showcase/search-index.json` by **parsing the real page markup** (`scripts/build-search-index.mjs`), so a card renamed in HTML cannot drift out of sync with what search returns; `npm run check:search-index` fails CI if it is stale.\n- **Section purpose copy** — every section header and category page now states what the group of components *is* and what job it does for the user, derived from the shipped source and the four pillars in `Tabatha_Concept.md`.\n- **Per-card Report bug / Request feature** — every one of the 90 cards carries both actions, bound to that exact component (id + name + page prefilled into the form). Injected at runtime from the card's own `id` and caption, so no per-card markup was needed. The controls are absolutely positioned and transparent until hover/focus, which keeps `.libcard` bounding boxes **byte-identical for the card captures**.\n- **Feedback modal + `POST /api/feedback`** — a Cloudflare Pages Function (`functions/api/feedback.js`) files submissions as Asana tasks, reading its token from the `ASANA_TOKEN` env binding (never hardcoded, never logged). With no token configured it returns a clear **501** and the client falls back to opening a **prefilled GitHub issue**, so the feature works before the backend is wired. Also the fallback on `file://` and on any upstream error: the reporter's words are never lost.\n- **Header \"Request a feature\"** — the same modal from every page, with no component prefilled.\n- **Roadmap page** (`showcase/roadmap.html`) — a kanban board whose columns are the product's **real funnel stages** (`FUNNEL_STAGES` in `src/hooks/useFocusEngine.js`: unsorted, todo, focus, addressing, roadblocked, resolved) with their shipped labels, emoji and colors. Fed by a **curated** `showcase/roadmap.json` (34 entries) — deliberately not wired to the internal board, so no internal task or client names can leak.\n- **Cloudflare Pages deployment** — `showcase/_headers` (CSP, no external origins, `frame-src 'self'` for the embedded surfaces), plus deploy, env-var and roadmap-update docs in `showcase/README.md`. `npm run site:serve` serves `showcase/` over http locally (search and the roadmap need a real origin) and stubs `/api/feedback` at 501 to exercise the fallback."
        },
        {
          "label": "Fixed",
          "body": "- **Showcase search: HTML entities rendered literally** — card titles containing `&mdash;` were indexed raw, so results read `Gatekeeper &mdash; agent` and a search for `gatekeeper agent` could not match across the entity. The index builder now decodes named, decimal and hex entities."
        },
        {
          "label": "Notes",
          "body": "- The 8 surface frames are unchanged and carry **no site chrome**. They are rendered at an exact 1280×800 as the whole viewport by the capture script, so adding a header or search box to them would corrupt the five Chrome Web Store screenshots. Their purpose copy lives on the hub, where they are presented."
        }
      ]
    },
    {
      "version": "6.7.17",
      "title": "Chrome Web Store API publishing pipeline",
      "date": "2026-07-16",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **`scripts/cws-auth.mjs` + `npm run cws:auth`** — one-time OAuth bootstrap for the CWS Items API: auto-locates the downloaded \"Tabatha CWS Publisher\" `client_secret_*.json` in Downloads, runs a local loopback consent flow (opens the default browser, no code-pasting), and writes `CWS_CLIENT_ID` / `CWS_CLIENT_SECRET` / `CWS_REFRESH_TOKEN` into the gitignored `deploy-creds.local`.\n- **`scripts/cws-publish.mjs` + `npm run cws:upload` / `cws:publish` / `cws:status`** — the release pipeline: mints an access token from the stored refresh token, uploads `store-assets/tabatha-store-v<version>.zip` (building it via `build:store` first if missing) to a new (`--new`, writes `CWS_APP_ID`) or existing item, publishes via `publishTarget` (defaults to `trustedTesters` for the staff rollout; `--target default` for the wider unlisted audience), and reports draft `--status`. Surfaces the API's own `itemError` messages verbatim; never logs a token value.\n- **`scripts/lib/deploy-creds.mjs`, `scripts/lib/cws-client.mjs`, `scripts/lib/cws-args.mjs`, `scripts/lib/cws-zip.mjs`** — pure, unit-tested helpers backing both scripts (creds file parse/merge round-trip, client_secret discovery/parsing, argv parsing, store-zip path resolution).\n- **`docs/cws-api-release.md`** — the runbook: one-time auth, the per-release loop (bump → build:store → upload → publish), the `publishTarget` vs. Visibility distinction, and the review-latency expectation for the `identity` + `<all_urls>` permission combination."
        }
      ]
    },
    {
      "version": "6.7.16",
      "title": "Showcase expansion: full component inventory + card captures",
      "date": "2026-07-16",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Showcase expanded to the whole user-facing surface area** — `showcase/index.html` is now an indexed gallery (sticky in-page nav, grouped sections, client-side filter) fronting **8 surface frames** and **6 new category pages** carrying **90 component cards**: `components-overlays.html` (InBar in 5 states, sticky note, edit dropdown, backburner, Gatekeeper strict/relaxed/inherited/agent, BlockGate, Welcome Back, What's New), `components-focus.html` (FocusBar active/paused/drifted/over-time/edit/checkpoint, FocusInput, queue, backburner dock, CheckpointTimeline, FlipClock, shift controls), `components-data.html` (every hand-rolled chart incl. the 365-day ActivityHeatmap and LogsPanel), `components-org.html` (TeamActivity, StatusChip's 9 states, invites, org tree, Live Stints, abandoned stints, Schedule's 3 modes), `components-settings.html` (all 22 shipped sections, sync chip's 5 states, SettingsSearch, all 12 themes, URL Rules, Developer, Cortex), and `components-primitives.html` (GlassCard, PopButton, ComboInput, TagPicker, CommandPalette, KeyboardShortcuts, LinkMergeModal, UnifiedTimeline). State variants are shown side by side wherever state is the point.\n- **New surface frames** — `popup.html` (the 400×500 Quick Switch popup), `workshifts.html` (Work Shifts → Analytics), and `settings-sections.html` (all 22 sections + the live-preview column).\n- **`npm run capture:showcase`** (plus `capture:shots` / `capture:cards`) — `scripts/capture-screenshots.mjs` now captures all 8 shot frames **and** every `.libcard` to `store-assets/screenshots/components/<page>--<card>.png` via CDP, validating each for dimensions and non-blankness. 98 assets in ~28s. The five Chrome Web Store shots keep their exact names and 1280×800 dimensions, now enforced by an explicit `cws` flag."
        },
        {
          "label": "Fixed",
          "body": "- **Showcase: invisible bars in the Work Shifts weekly and per-focus charts** — the fills are `<span>`s, so `height:100%` was silently ignored on an inline box and the bars rendered as empty tracks. They are `display:block` now.\n- **Capture: a fixed CDP debug port made captures attach to the wrong browser** — an orphaned headless Chrome holding port 9333 meant every navigate/screenshot targeted a stale page (capture ran ~500× slower and produced blank cards). Chrome now picks a free port and reports it via `DevToolsActivePort`."
        }
      ]
    },
    {
      "version": "6.7.15",
      "title": "Component showcase / display pages",
      "date": "2026-07-15",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **`showcase/` component showcase** — polished, on-brand, self-contained HTML display pages for every key surface (Gatekeeper, sidebar, New Tab dashboard, Sync & Account settings, backdating / checkpoint timeline) plus a component library (InBar, BlockGate, TagPicker, tab rows, analytics, abandoned-stint modal). A reusable marketing / docs / onboarding asset rendered from the real design system (halved radius scale, Pop-Art palette, emoji iconography). Five pages double as the Chrome Web Store screenshots; `scripts/capture-screenshots.mjs` renders them to `store-assets/screenshots/*.png` at an exact 1280×800."
        }
      ]
    },
    {
      "version": "6.7.14",
      "title": "First complete self-hosted channel build",
      "date": "2026-07-15",
      "intro": "",
      "sections": [
        {
          "label": "Changed",
          "body": "- First update-channel release built from the fully-merged line — combines the self-hosted updater (v6.7.13) with the backdating-actually-moves fix (v6.7.12), which the initial v6.7.13 release predated. This is the build the auto-updater serves to staff installs."
        }
      ]
    },
    {
      "version": "6.7.13",
      "title": "Self-hosted remote update channel for unpacked staff installs",
      "date": "2026-07-15",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **`scripts/publish-update.mjs` + `npm run publish:update`** — builds the WITH-key unpacked-staff package, zips `dist/` to `store-assets/tabatha-<version>.zip`, computes its sha256, publishes a GitHub Release (tag `ext-v<version>`, distinct from the project's `v<version>` tag scheme) with the zip attached, and commits `latest.json` (`{ version, zipUrl, sha256, published }`) to a dedicated `update-channel` branch — served at the stable URL `https://raw.githubusercontent.com/MrMalkio/tabatha/update-channel/latest.json`.\n- **`scripts/tabatha-updater.ps1`** — polls that `latest.json`, compares semver against the installed stable-path manifest, and on a newer remote version: downloads the release zip, verifies its sha256 (aborts and leaves the current install untouched on any mismatch), validates the extracted build (manifest parses, pinned key present, entry pages present), then atomic-swaps it into `%LocalAppData%\\Tabatha\\extension` using the same never-empty rename pattern as `mirror-extension.ps1`. Logs to `%LocalAppData%\\Tabatha\\update.log`. Idempotent — no-op when already current.\n- **`scripts/install-tabatha-staff.ps1`** — staff/multi-machine installer superseding `install-extension-persistence.ps1` for non-dev machines: seeds the stable path from a bundled `.\\extension` payload, registers both an HKCU Run key (logon) and a Scheduled Task (every 6h) running the updater, and prints the one-time \"Load unpacked\" instruction.\n- **`store-assets/tabatha-staff-v<version>.zip`** — refreshed staff bundle: built extension (WITH pinned key) + `tabatha-updater.ps1` + `install-tabatha-staff.ps1` + updated `INSTALL.md` documenting the 6h/logon auto-update cadence.\n- This channel does NOT depend on Chrome Web Store review — it is live and testable on any staff machine (e.g. PS) the moment `npm run publish:update` is run once."
        }
      ]
    },
    {
      "version": "6.7.12",
      "title": "Backdating actually moves the start; overlap is reported, not blocked",
      "date": "2026-07-15",
      "intro": "> Supersedes v6.7.10's clamp-feedback behavior: the anti-double-count clamp against other focuses is **removed**, not just explained.",
      "sections": [
        {
          "label": "Fixed",
          "body": "- **Backdating a start time now actually moves the start — even when other focuses occupied that window.** The anti-double-count guard in `SET_FOCUS_START_TIME` treated every other focus (including the entire paused/queued backlog) as time already claimed and \"resolved\" overlap by shoving the proposed start *forward* to the latest interval end — with a full queue, that collapsed nearly every backdate into a silent no-op. The start the user picks now always takes effect, bounded only by two hard limits: not before clock-in, not in the future. Parallel/overlapping focuses are an accepted concept — overlap with another focus is now **reported, not silently blocked**: the editor confirms e.g. `Backdated to HH:MM — overlaps \"Email triage\" by 20m (both keep their time)`, and the timeline checkpoint notes the overlapped minutes.\n- **Edits limited by the hard bounds explain themselves.** A backdate fully swallowed by the clock-in floor or the now ceiling keeps the editor open with why and the earliest available time; a partially bounded one confirms the effective time and credited minutes; a clean backdate briefly confirms `+Xm credited`. (Per-focus accounting is unchanged: credited `elapsedMs` is still capped at wall-clock since the new start.)"
        },
        {
          "label": "Changed",
          "body": "- **`validateStartTime` contract:** bounds the start to `[clock-in, now]` only; `clamped`/`clampedBy` now reflect only those bounds (`'clock-in'` / `'now'`), and overlapping other-focus intervals are returned as `overlaps` (with per-interval `overlapMs` and the sibling's `label`) for the caller to surface. `SET_FOCUS_START_TIME` response adds `overlaps` alongside `startedAt`/`addedMs`/`clamped`/`clampedBy`. A follow-up will let the user trim the overlap from the other focus or move it to backburner time."
        }
      ]
    },
    {
      "version": "6.7.11",
      "title": "Chrome Web Store packaging: real icons, promo tile, store zip, privacy policy",
      "date": "2026-07-15",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **True multi-resolution icons.** `public/icons/icon16/32/48/128.png` are now real 16/32/48/128px exports (previously all three shipped the same mislabeled 1024×1024 PNG). The 1024px original is preserved as `public/icons/icon-1024.png`; the manifest (`icons` + `action.default_icon`) now also declares the 32px size.\n- **`scripts/build-store-zip.mjs` + `npm run build:store`** — builds, stages `dist`, strips the pinned `key` from the staged manifest (Chrome Web Store rejects uploads carrying one), validates the payload (entry pages, icons, no sourcemaps/dotfiles), and zips to `store-assets/tabatha-store-v<version>.zip`.\n- **Store promo tile** — `store-assets/promo-440x300.png` (icon + wordmark + tagline on brand dark).\n- **`PRIVACY.md`** — plain-language privacy policy (from the listing doc's disclosure section), hosted publicly for the store's privacy-policy URL requirement.\n- **Staff interim bundle** — `store-assets/tabatha-staff-unpacked-v<version>.zip` (built extension WITH pinned key + persistence installer + 3-step INSTALL.md) so existing unpacked installs keep their extension ID until the store listing is live."
        }
      ]
    },
    {
      "version": "6.7.9",
      "title": "Stable extension load path (stops \"uninstalling on restart\")",
      "date": "2026-07-15",
      "intro": "",
      "sections": [
        {
          "label": "Fixed",
          "body": "- **The extension no longer disappears after a restart.** Chrome loaded the unpacked extension straight from the git build folder (`…\\Tabatha\\dist`), which `npm run build` legitimately empties/rewrites. Any restart — made frequent and unattended by Windows Fast Startup, and Chrome's startup extension-GC after an unclean exit — that landed while `dist` was mid-build/invalid caused Chrome to permanently drop the unpacked entry, forcing a manual \"Load unpacked\". The load path is now decoupled from the git tree: a stable mirror at `%LOCALAPPDATA%\\Tabatha\\extension` is kept populated with the freshest **valid** build and self-heals on logon, so Chrome always validates against a complete folder."
        },
        {
          "label": "Added",
          "body": "- **`scripts/mirror-extension.ps1`** — atomically mirrors a valid `dist` build into the stable load path (validates manifest + pinned key + entry pages before swapping; leaves the last-known-good copy untouched if the source is missing/mid-build).\n- **`scripts/install-extension-persistence.ps1`** — one-time machine setup: seeds the stable path and registers a logon autostart (scheduled task, or HKCU Run-key fallback) that re-mirrors/self-heals before Chrome starts. Prints the one-time \"Load unpacked from the stable path\" step.\n- **`npm run mirror:extension`** — refresh the stable load path on demand after a build."
        }
      ]
    },
    {
      "version": "6.7.8",
      "title": "Sync review fixes: durable fallback + per-op outbox persistence",
      "date": "2026-07-10",
      "intro": "",
      "sections": [
        {
          "label": "Fixed",
          "body": "- **Display-name saves can no longer silently vanish.** If the service worker was asleep or torn down mid-message (a routine MV3 race), `saveDisplayName` used to swallow the failed `sendMessage` into a fabricated `{ ok: true }` — nothing was queued, no retry armed, and the cloud row stayed stale with a \"✓ syncing\" message. `updateProfileName` now falls back to persisting the write directly into the durable outbox (`_cloudOutbox`) and arms the flush alarm; only a genuine local-persist failure surfaces a real, retryable error to the user.\n- **Cloud outbox honours its at-most-once-per-success contract under teardown.** `flushCloudOutbox` now persists the queue after each op's success/failure inside the loop instead of once at the end, so a service-worker teardown mid-flush can't re-execute an op whose network write already landed."
        }
      ]
    },
    {
      "version": "6.7.7",
      "title": "Background-routed cloud writes + outbox queue (page auth hang fixed)",
      "date": "2026-07-10",
      "intro": "",
      "sections": [
        {
          "label": "Fixed",
          "body": "- **Root-caused and killed the page-context Cloud Sync hang.** Saving a display name, creating an organisation, or redeeming/minting an invite from Settings (and Work Shifts schedule reads) could hang ~10–15s then time out, and the Account panel logged `auth_init_failed`. The cause was a self-deadlock inside the page's auth client: its init lock is held while `onAuthStateChange` subscribers run, and the subscriber's nested cloud read re-entered that same lock forever. The `onAuthStateChange` callback now defers all follow-up work off its microtask (never `await`s a cloud call inline), so the client can never wedge."
        },
        {
          "label": "Added",
          "body": "- **Background is now the single Cloud auth owner.** Page contexts source the session and access token from the background service worker (`GET_AUTH_STATE` / `GET_ACCESS_TOKEN`) and read via a deadlock-proof data client configured with an `accessToken` callback — it never runs page-local auth machinery. Realtime (team activity, live profile) keeps working on the routed token.\n- **Cloud outbox — queue, never race.** Display-name changes enqueue to a durable, idempotency-keyed queue (`src/utils/cloudOutbox.js`) with latest-wins dedupe and exponential backoff, flushed by the background. The UI gets an instant optimistic ack instead of a 10s timeout race; the name updates locally, survives a reload, and reconciles on cloud confirm. Survives service-worker restarts.\n- **All cloud mutations routed through the background** (`UPDATE_PROFILE_NAME`, `CREATE_ORGANIZATION`, `REDEEM_INVITE_TOKEN`, `CREATE_INVITE_TOKEN`, `DELETE_INVITE_TOKEN`) so they execute against the never-wedged background client.\n- **Auto-sync on sign-in.** Signing in now triggers a full sync + outbox flush automatically (also on service-worker boot) — no need to click \"Cloud Sync now\" (kept as a manual fallback)."
        }
      ]
    },
    {
      "version": "6.7.6",
      "title": "UI: hide backend vendor name in Cloud Sync label",
      "date": "2026-07-10",
      "intro": "",
      "sections": [
        {
          "label": "Changed",
          "body": "- **UI:** renamed the \"Supabase Cloud Sync\" integration label to \"Cloud Sync\" (hide backend vendor name from end users)."
        }
      ]
    },
    {
      "version": "6.7.5",
      "title": "Session-log housekeeping",
      "date": "2026-07-10",
      "intro": "",
      "sections": [
        {
          "label": "Changed",
          "body": "- Docs/session-log housekeeping; version discipline reconciliation."
        }
      ]
    },
    {
      "version": "6.7.3",
      "title": "Cortex capture-config reconnect-flap guard",
      "date": "2026-07-10",
      "intro": "",
      "sections": [
        {
          "label": "Fixed",
          "body": "- **A settings read racing an MV3 SW-restart storm could silently flap desktop capture off** for as long as ~90 seconds. `pushCaptureConfig()` ran unconditionally on every companion-bridge reconnect; since `getSettings()` always merges onto `DEFAULT_SETTINGS` (`screenshotCapture: false`), a read before the raw `settings` key was confirmed loaded resolved to \"off\" and pushed that guess to the companion. Added `isSettingsLoaded()` — a pure guard that distinguishes \"genuinely not configured yet\" from \"confirmed loaded\" — so the push is skipped rather than asserting a false negative."
        }
      ]
    },
    {
      "version": "6.7.2",
      "title": "Cortex panel: surface the real capture folder",
      "date": "2026-07-10",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **\"Where are my captures?\"** — the Cortex panel now shows the desktop companion's actual on-disk frames directory and a live \"last frame written\" timestamp, instead of only the extension's own observation-ledger count. The companion's `GET_CAPTURE_STATE` reply is now persisted (`companionCaptureState`) and re-requested on every bridge reconnect via a new `GET_COMPANION_CAPTURE_STATE` handler."
        }
      ]
    },
    {
      "version": "6.7.1",
      "title": "Awareness identity-race fix (Live Stints ghost cards)",
      "date": "2026-07-10",
      "intro": "",
      "sections": [
        {
          "label": "Fixed",
          "body": "- **Live Stints ghost cards / silent no-ops on cold start.** `LIST_LIVE_STINTS`, `GET_OTHER_QUEUE`, `CLOCK_OUT_INSTALL`, `DISMISS_INSTALL`, `CLEAR_ALL_OFFLINE`, and the auto-clock-in abandoned-stint check all hard-guarded on the module-level identity `startAwareness()` sets — before that resolves (fresh service-worker spin-up, or a restart mid-startup), they silently returned empty. Added `resolveActiveIdentity()`, a fallback that re-derives the profile/install identity directly from the Supabase session when module state isn't populated yet, and wired it into all five call sites plus `getOwnAbandonedStints()`."
        }
      ]
    },
    {
      "version": "6.7.0",
      "title": "Work Schedule Profiles (NB-01/NB-02)",
      "date": "2026-07-10",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Work Schedule Profiles**: per-member `dedicated_hours` / `self_managed` work profiles, required-hours floors at daily/weekly/monthly cadences (independent, anti-back-loading), a fixed weekly schedule editor, and a schedule-change-request flow (slot change / shift hours / make-up) with a manager approval inbox.\n- **Shortfall tracking**: a `shortfall_ledger` records under-hours periods at clock-out (fire-and-forget, fail-open when signed out/offline) with resolutions (`unresolved` / `made_up` / `shifted` / `excused`) and a `SHORTFALL_PROMPT` notification.\n- **Work Shifts → Schedule view**: three modes — **Self** (own schedule, floors, shortfall accounting, change requests), **Manage** (roster, profile type, slots, floors — gated by org role/permissions), and **Requests** (approval inbox). The legacy local `workSchedule` key is kept as an offline cache and signed-out editor.\n- **Migration 027** (renumbered from the branch's original 023; applied): `org_members.work_profile_type`, `work_requirements`, `work_schedule_slots`, `schedule_change_requests`, `shortfall_ledger`, plus 4 hardened `SECURITY DEFINER` RPCs (`set_member_schedule`, `set_work_requirements`, `set_member_work_profile`, `decide_change_request`) — `search_path=''`, schema-qualified, `REVOKE PUBLIC/anon`, `GRANT authenticated`."
        }
      ]
    },
    {
      "version": "6.6.0",
      "title": "Org management foundation, real analytics, time-editing overhaul + critical storage fix",
      "date": "2026-07-06",
      "intro": "",
      "sections": [
        {
          "label": "Fixed",
          "body": "- **CRITICAL — pause/resume (and all state changes) silently failing**: the extension's `chrome.storage.local` hit Chrome's 10 MB quota, so every state-changing write was rejected while reads kept working — the UI looked alive but nothing persisted. Added the `unlimitedStorage` permission, and storage write failures now surface loudly (console + notification) instead of being swallowed. (Requires the one-time extension reload that delivers this version.)\n- **Sync survives schema drift**: if the client is ever ahead of the database (a missing column), sync now retries that block without the column and reports a named diagnostic instead of failing outright."
        },
        {
          "label": "Added",
          "body": "- **Work Shifts → Analytics is real** (for everyone, including personal installs): daily hours, weekly comparison, peak productivity hours, break patterns, time-per-focus during shifts, context-switching frequency, and CSV export — all computed from your own data. Org/cross-member analytics ships next (org admins only).\n- **Org roles foundation**: owner/admin are org-wide; managers are scoped to their own teams. New permission helpers gate every org-management surface. (The full Work Schedule + work-profiles system is built and lands in the next update, once its database migrations are applied.)\n- **Time editing that actually works**: type durations like \"500m\" or \"8h20m\" (set total / add / subtract), live preview incl. clamp warnings, Enter-to-apply, edit panel available even with no checkpoints, and honest feedback when a limit clamps your edit.\n- **Offline-gap detection**: if the machine was off/asleep while an intent was running, Tabatha now detects the gap on wake, retro-pauses at the gap start, and asks you to confirm/credit/adjust — no more phantom hours.\n- **Abandoned-stint prompt at clock-in**: your own orphaned stints surface when you clock in, with an end-time picker to correct them on the spot.\n- **Settings search**: press \"/\" in Settings and fuzzy-find any option; sync/companion status chips now click through to Sync & Account."
        }
      ]
    },
    {
      "version": "6.5.0",
      "title": "Dogfood fix batch: clock sync, onboarding, crash-hardening + new features",
      "date": "2026-07-01",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Create Organization** (Settings → Sync & Account): self-serve org creation via a new `tabatha.create_organization` SECURITY DEFINER RPC (migration 020) that atomically creates the org, an owner membership, and stamps the profile default — closing the chicken-and-egg gap where the very first user of an org had no way to bootstrap one. (FIX-09)\n- **Cross-device intent queue**: device chips in the awareness strip now expand to show each other signed-in machine/profile's **read-only** intent queue, with P-priority synced (migration 021 adds `focus_items.priority`). (FIX-10)\n- **\"What's New\" update popup + changelog**: a one-time-per-version modal summarizing changes on upgrade (generated from this changelog into `public/changelog.json`), plus a full Changelog view in Settings → About. (FIX-11)\n- **Configurable toolbar-icon click**: choose whether clicking the toolbar icon opens the side panel (new default) or the tab-list popup, plus a rebindable `open_tab_list` keyboard shortcut. (FIX-12)\n- **Companion (Tabby Desk):** file-based crash logging + global panic hook, and user-visible \"Check for Updates\" feedback via the tray. (FIX-04/06)"
        },
        {
          "label": "Fixed",
          "body": "- **Companion → extension clock sync**: the companion's clock state was written to the wrong storage key and never reached the `clockSession` that Home reads (plus a snake/camel shape mismatch), so tray/Desk-panel clock in/out never showed on Home. Now mapped and persisted correctly. (FIX-02/05)\n- **Invite-join was silently broken**: the `redeem_invite_token` RPC call wasn't schema-qualified (`.schema('tabatha')`) → PGRST202. Redeeming an invite token now works. (FIX-09)\n- **Companion crashes / \"vanishing\"**: a poisoned-`Mutex` cascade (`.lock().unwrap()` everywhere) took the whole process down after any single panic. Replaced with poison-recovering locks, a `catch_unwind`-wrapped monitor loop, and panic-hook file logging. (FIX-06)\n- **Activity Log** opened a broken terminal (`cmd /c \"type … & pause\"`) and could take the companion down; now opens the report via the OS default handler from the app-data `logs/` folder. (FIX-03)\n- **Intent queue** no longer disappears when there is no active intent — it always renders, with a muted empty state. (FIX-08)\n- **External Integrations panel** now reflects the live Desktop Companion connection (`companionConnected`) instead of a never-written flag that always read \"Not configured\". (FIX-01)"
        },
        {
          "label": "Changed",
          "body": "- **Toolbar default**: clicking the extension icon now opens the **side panel** by default (previously the popup); the popup is reachable via the setting or the new hotkey. (FIX-12)\n- **Companion UI copy**: \"Debug Panel\" → \"Desk Panel\", \"Tabatha Desktop\" → \"Tabby Desk\", \"Tracking\" → \"Tabbing\"; the version is now read from the build instead of hard-coded. (FIX-07)\n- **Companion release profile**: `panic = \"abort\"` → `\"unwind\"` so crash-containment (`catch_unwind` / poison recovery) is actually effective. (FIX-06)\n- **Migrations**: 020 (`create_organization` RPC + `org_members.role` CHECK reconciled to include `owner`, hardened `search_path = ''`), 021 (`focus_items.priority` column). Authored + Koda-reviewed; applied at deploy."
        }
      ]
    },
    {
      "version": "6.4.0",
      "title": "Ghost-stint fix: durable install identity + Live Stints panel",
      "date": "2026-06-04",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Live Stints panel** (Work Shifts → Live Stints view): lists all browser-profile installs currently clocked in — self, live siblings, and dead orphans — with per-install badges (live/stale/machine group), clock-in timestamp, last-seen heartbeat, and individual **Clock out** / **Dismiss** actions. Dead orphans show an end-time picker prefilled with `last_heartbeat_at`. Bulk **Clean up all offline** removes all stale presence rows in one step.\n- **`OtherProfilesStrip` stale-collapse**: the awareness strip now shows only live chips; stale orphans collapse into a single `🕘 N offline · clean up` chip that links to the Live Stints panel.\n- **Home clock indicator**: a compact chip near the home clock links to the Live Stints panel when sibling stints are present.\n- **New background handlers** in `awarenessService`: `LIST_LIVE_STINTS`, `CLOCK_OUT_INSTALL`, `DISMISS_INSTALL`, `CLEAR_ALL_OFFLINE`.\n- **Self-command listener** on the Supabase Realtime subscription: a live sibling install can be clocked out remotely by writing `metadata.clock_out_requested_at`; the target install runs its own local clock-out sequence.\n- **Pure reconciliation helpers** (`src/utils/stintReconciliation.js`): `isLiveConcurrent`, `reconstructStintFromStatus`, `resolveAttributionTarget`, `classifyInstallForCleanup`. 26 unit tests."
        },
        {
          "label": "Fixed",
          "body": "- **Concurrent-shift false warning**: the \"hours may stack and double-count\" popup now only fires for a genuinely live, same-classification install. Dead orphans (stale/offline) and installs with a different classification (personal vs professional) no longer trigger the warning."
        },
        {
          "label": "Changed",
          "body": "- **Durable install identity** (migration 017): `browser_profiles` gains `local_id` (stable per-install UUID) and `machine_id` (companion browser-profile link). A unique index on `(profile_id, local_id)` makes `ensureBrowserProfileRow` idempotent — the same install always maps to one row, regardless of concurrent syncs or storage resets. Existing rows keep `local_id IS NULL` and are reconciled via the Live Stints panel."
        }
      ]
    },
    {
      "version": "6.3.2",
      "title": "QA regression fixes (idle overlay, sidebar parity, domain backfill)",
      "date": "2026-05-29",
      "intro": "",
      "sections": [
        {
          "label": "Fixed",
          "body": "- **D1 — Idle overlay was dismissed when user moved their mouse.** Chrome fires `idle→active` when mouse moves, which was broadcasting `IDLE_PROMPT_RESOLVED` and removing the overlay. Now only dismissed on `timeout` (the hard-pause fallback). The user can move their mouse and click a response button.\n- **G — Domain Groups tab was empty on first load.** `recordDomainVisit` only fires on new navigations; existing tracked tabs were never recorded. Background now scans `chrome.storage.local.tabs` on startup and backfills the domain history.\n- **B7 — Meeting-domain textarea couldn't start a new line.** `onChange` was calling `filter(Boolean)`, deleting empty lines immediately when Enter was pressed. Now uses local draft state, saved to settings on blur.\n- **Notification icons** — `icons/icon128.png` relative path fails in service worker; replaced with `chrome.runtime.getURL(...)`.\n- **Timeline note text** — multi-line notes now render with `white-space: pre-wrap` instead of collapsing to one paragraph."
        },
        {
          "label": "Added",
          "body": "- **Sidebar parity** — Sidebar active-focus card now has 📊 checkpoint timeline (full edit mode), 📱/📴 off-device toggle, and 📌 Sub-focus button.\n- **`CheckpointTimeline` shared component** (`src/components/CheckpointTimeline.jsx`) used by both home FocusBar and sidebar.\n- **Auto-focus chip** fade extended 8s → 20s (chip was disappearing before user could dismiss InPop)."
        }
      ]
    },
    {
      "version": "6.3.1",
      "title": "Self-review fix: stale meeting tab no longer disables idle",
      "date": "2026-05-29",
      "intro": "",
      "sections": [
        {
          "label": "Fixed",
          "body": "- **Forgotten meeting tab permanently disabled idle detection.** `isUserInMeeting()` treated any meeting-domain tab open >2min as an active call, so a Zoom/Meet tab left open all day suppressed idle pausing indefinitely. The \"muted backgrounded meeting\" signal is now bounded to the meeting grace window (`meetingIdleGraceMinutes`, default 60m) and an explicit active-tab signal was added. Recently-joined muted meetings are still detected; stale tabs are not. (Found in a pre-PR self-review; guarded by new regression tests.)"
        }
      ]
    },
    {
      "version": "6.3.0",
      "title": "Plan 037 Phase 2 Checkpoint Timeline Editing",
      "date": "2026-05-29",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Timeline edit mode**: the checkpoint timeline (📊) now has an **✏️ Edit / ✓ Done** toggle. In edit mode you can:\n  - Edit any note's text and progress level inline (✏️ per entry).\n  - Delete any entry (✕), including auto-generated system entries.\n  - Add a new checkpoint note (+ Add checkpoint note).\n  - Adjust tracked time with clearer UI — the total is shown prominently with an explanation, so the time controls are no longer cryptic.\n- New handlers `EDIT_CHECKPOINT` and `DELETE_CHECKPOINT` (focusService)."
        },
        {
          "label": "Changed",
          "body": "- The \"Adjust tracked time\" controls now live inside Edit mode with a labelled total and helper text (previously an unexplained button row)."
        }
      ]
    },
    {
      "version": "6.2.0",
      "title": "Plan 038 Phase 1 Persistent Domain Store",
      "date": "2026-05-29",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Persistent domain history** (`domainHistoryService`): Tabatha now permanently remembers every domain (and its path variations, visit count, and observed intents) you visit — independent of whether the tab is still open. Capped at 2000 domains (configurable), LRU-evicted. This is the data backbone for building URL rules at any time, not just for currently-open sites.\n- **Settings → URL Rules → Domain Groups** rebuilt on the persistent store: search/filter, visit count, last-seen, live-open indicator, and per-domain **⭐ Target** (prompt me to make a rule next visit), **🚫 Dismiss** (hide + stop prompting), and **↩ Restore** actions. A \"Show dismissed\" toggle reveals hidden domains."
        }
      ]
    },
    {
      "version": "6.1.0",
      "title": "Plan 037 Focus Time Editing + Plan 036 QA fixes",
      "date": "2026-05-29",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Focus time editing** (Plan 037): the checkpoint timeline (📊) gains a \"🛠 Adjust tracked time\" row — quick deltas (−5m/−1m/+1m/+5m), a set-exact-minutes input, and **Remove last pause** (restores the time a pause ate and reactivates the focus). New handlers `ADJUST_FOCUS_TIME`, `SET_FOCUS_ELAPSED`, `REMOVE_LAST_PAUSE`, all clamped to wall-clock time and audit-logged.\n- **Auto-pause master toggle** (`autoPauseEnabled`): Settings → Focus Lifecycle. When off, going idle never mutates any focus — not even a prompt."
        },
        {
          "label": "Fixed",
          "body": "- **Off-device focuses are no longer paused by idle.** Both `handleIdleStateChanged` and `hardPauseActiveFocus` now exempt `offDevice` focuses, fixing the time-data corruption where an off-device focus was silently paused while the user worked outside Chrome.\n- **Tooltips no longer clip off-screen.** Tooltip x-position is clamped to the viewport and long text wraps (max 280px) — fixes the cut-off tooltips in Settings and everywhere else.\n- **Meeting-domain editor**: the textarea now splits on newlines only (not commas), shows a format placeholder + Enter hint, and stops host-page keyboard shortcuts from intercepting input."
        }
      ]
    },
    {
      "version": "6.0.0",
      "title": "Plan 036 Intelligent Focus Lifecycle — Smart Idle + Auto-Focus + Drift",
      "date": "2026-05-29",
      "intro": "Absorbs Plans 026 (Auto Focus) and 029 (Auto-Pause Overhaul). Resolves bugs B05/B08 and addresses features #149–#152, #187. Incorporates the challenge-audit resolutions (multi-profile sync race, meeting detection, drift false-positives, prompt-storm mitigation).",
      "sections": [
        {
          "label": "Added",
          "body": "- **Smart Idle Engine** (`clockService`): before pausing the global focus, `collectIdleSuppressors()` consults (a) other browser profiles via the awareness cache, (b) desktop-companion activity within a configurable grace window, and (c) a hardened 3-layer `isUserInMeeting()` scan (all open tabs + companion app), so muted/backgrounded meetings and cross-profile activity no longer trigger false pauses. When idle is genuine, an `IDLE_PROMPT` is shown (\"Yes, on task\" / \"I diverged\" / \"Pause focus\") instead of a silent hard-pause; an unanswered prompt falls back to a hard pause after 5 minutes. Legacy hard-pause is still available via the \"Prompt before pausing\" toggle.\n- **Multi-profile idle state** (`awarenessService`): each profile now publishes its Chrome idle verdict in the `browser_profile_status.metadata` jsonb (no schema migration) and surfaces it in the `_otherProfiles` cache, fixing the multi-profile sync-override hazard where an unattended profile could pause an actively-worked focus account-wide.\n- **Auto-Focus heuristic engine** (`autoFocusService`): when no focus is active, tab activations/navigations are matched against URL rules (explicit auto-create), category/domain groups (high confidence), and the companion app category (medium), surfacing a non-blocking InBar chip that auto-fades after 8s. A per-domain exponential decay engine (30→60→120→240→480m) suppresses repeat prompts after dismissal.\n- **Context drift detection** (`autoFocusService`): a 5-layer association hierarchy (direct association, companion overrule, URL-rule intent, category/domain-group, hostname) plus a localhost/chrome:// whitelist decides whether a tab counts as drift. A wandering→drifted state machine (armed by the `auto-focus-drift` alarm at `driftThresholdMinutes`) raises `FOCUS_DRIFT_DETECTED` with \"Still working / Switching / Just checking\" options and emits a `context_drift` webhook.\n- **Auto clock-in (#187)**: optional, with a configurable trigger — \"When Chrome opens\" (default, via `runtime.onStartup`) or \"On OS unlock\" (via the desktop companion's idle→active transition).\n- **Companion bridge helpers** (`companionService`): `lastHeartbeat`, `getActiveApp`/`getActiveAppCategory`, and `isRecentlyActive(grace)` for the idle and drift engines.\n- **Settings → 🧠 Focus Lifecycle** panel: idle behaviour (prompt toggle, thresholds, companion grace, meeting-domain editor), auto-focus (enable, confidence, dismissal-history viewer/clear), drift detection (enable + thresholds), and auto clock-in (enable + trigger). URL Rules gain a per-rule **🎯 Auto-create focus** toggle."
        },
        {
          "label": "Changed",
          "body": "- Idle handling no longer mutates the global focus when the user is active on another profile, in another desktop app, or in a meeting — it only updates this profile's own status."
        }
      ]
    },
    {
      "version": "5.8.0",
      "title": "Plan 031 Gap Completion — Auto-Checkpoint + SectionNav + Sub-Focus + Backburner Fixes",
      "date": "2026-05-28",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Auto-Checkpoint System** (`focusService.autoCheckpoint`): Lifecycle transitions (started, paused, resumed, completed, backburnered) now automatically generate system checkpoint entries. These appear in the Checkpoint Timeline with ⚙️ prefix at 60% opacity, distinguishable from manual user-submitted notes. System entries do not count toward the badge number or trigger stale-nudge timers.\n- **Sub-Focus Button** (FocusBar): `📌 Sub-focus` button in the FocusBar action row. Creates a child focus under the currently active parent. Queue items with a parent now display a purple `child` badge.\n- **SectionNav Sidebar Refactor**: Homepage navigation sidebar is now hover-expandable (44px icons → 160px icon+title on hover). Smart click logic: clicking an active section collapses it; clicking a different section navigates to it and auto-expands. Collapsed sections drop to the bottom of the sidebar with a divider and line-through styling. Collapsed section headers no longer render in the page body (zero-height anchor only), saving vertical real estate.\n- **Feature Spec #209** (`docs/features/209-focus-resolution-tab-cleanup.md`): Documented the Focus Resolution & Tab Cleanup feature for future implementation."
        },
        {
          "label": "Fixed",
          "body": "- **Backburner create-new-focus skeleton** (Critical): When backburnering a focus and choosing \"Create New\", the replacement focus object was missing 12 fields (`funnelStage`, `timerMinutes`, `createdAt`, `pausedAt`, `endedAt`, `overMs`, `parentFocusId`, `contextSwitchCount`, `priority`, `offDevice`, `lastCheckpointAt`, `checkpointSnoozedUntil`). These undefined values broke the priority dropdown, funnel stage picker, and timer display. Now uses the canonical field set matching `startFocus`.\n- **Backburner tab association inheritance**: Create-new-focus during backburner now inherits `associatedTabIds` from the backburnered focus and cross-links via `backburnerTransitionFocusId` for audit trail.\n- **`addFocus` missing priority**: The `addFocus` function (used when adding to queue) was missing the `priority` field, causing the priority selector in FocusQueue to render undefined. Now defaults to P5.\n- **Checkpoint badge count inflation**: The `📋 (N)` badge on the FocusBar was counting all checkpoint entries including system auto-entries, inflating the number. Badge now filters to user-only; timeline visibility uses total count.\n- **Video call idle suppression**: Idle detection now checks both audible meeting tabs (existing) AND the active tab's URL against meeting domain patterns (Meet, Zoom, Teams, WebEx). Prevents premature auto-pausing during muted calls or meeting waiting rooms.\n- **Regression RT-2**: LogsPanel type column added (replaces colored badge in details column).\n- **Regression RT-7**: Category override logic for domain-specific patterns (Meet → Communication, Scholar → Research)."
        }
      ]
    },
    {
      "version": "5.3.0",
      "title": "Plan 028 Phase D₂ — companion as a first-class install + clock-stacking warning + invite revocation",
      "date": "2026-05-19",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Desktop companion proxy-registers as a `browser_profiles` row** (`browser='desktop_companion'`). On every WebSocket connect the extension SELECTs-or-INSERTs the companion's row and caches its id locally as `_companionBrowserProfileId`. Migration 013 adds a partial unique index `(profile_id, browser)` WHERE `browser IN ('desktop_companion', 'mobile_ios', 'mobile_android', 'tabatha_web')` so concurrent races resolve cleanly.\n- **`companionInstallService`** heartbeats the companion's `browser_profile_status` row every 60 seconds while WS is connected; pushes immediately on `CLOCK_STATE` messages from the companion; flips `online=false` on disconnect. The awareness chip strip and Team Activity panel now render the companion alongside browser profiles (💻 icon).\n- **Clock-stacking warning.** Home dashboard and sidebar `CLOCK_IN` paths consult the cached `_otherProfiles` awareness data; if any other non-personal install is currently `clocked_in` or `on_break`, a confirm dialog lists them before allowing a second concurrent shift to start. Personal installs are excluded from the check because they have no clock.\n- **Pending Invites list** in Settings → Sync & Account → Team Activity. Org owners and managers see all unredeemed, unexpired tokens for their orgs with a per-row Revoke button (direct `DELETE` against `invite_tokens` — RLS gates server-side). The Generate Invite flow auto-refreshes the pending list on mint."
        },
        {
          "label": "Changed",
          "body": "- **Desktop activity attribution.** `syncService.buildDesktopRows` now stamps `desktop_activity` rows with the companion's `browser_profile_id` instead of the extension's whenever a companion install exists. Existing rows pre-D₂ remain attributed to the extension — that's acceptable; future companion activity is correctly scoped.\n- **Awareness chip icons** prefer `BROWSER_ICON[row.browser]` over `CLASSIFICATION_ICON[row.classification]` so the companion (💻), future mobile (📱), and web (🌐) installs are visually distinct from Chrome profiles."
        },
        {
          "label": "Migration",
          "body": "Run `supabase/migrations/013_companion_install_uniqueness.sql`. The unique index is partial — Chrome browser_profiles stay free-form so users with multiple Chrome profiles on one machine continue to work."
        },
        {
          "label": "Out of scope (still parked for Phase D₃+)",
          "body": "- Multi-machine companion support (today: one companion per user). A stable per-machine identifier on `profile_path` is the next step.\n- Mobile app(s) (iOS / Android) registering their own `browser_profiles` rows. The schema and unique index are already in place; the mobile clients need to do the upsert themselves.\n- Auto-update distribution via Chrome Web Store / signed CRX (Plan 019)."
        }
      ]
    },
    {
      "version": "5.2.0",
      "title": "Plan 028 first slice — Team Activity + Invite Mint + profile realtime",
      "date": "2026-05-19",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Team Activity panel** (Settings → Sync & Account) for org owners and team managers. Lists each member you can see, with their browser-profile installs and live awareness chips. Refreshes via Supabase Realtime on every status change.\n- **Generate Invite Token** mint UI. Org owners and team managers/sub-managers can mint tokens directly from Settings — pick org, optionally a team, role, and expiry hours; copy the resulting token. Pairs with the existing redemption flow.\n- **Manager RLS scoping** (migration 012) for `tabatha.browser_profiles`, `tabatha.browser_profile_status`, and `tabatha.profiles`. Org owners can read all members in their orgs; team owners/managers/sub-managers can read their team members. Insert/update/delete remain own-row.\n- **`tabatha.profiles` realtime subscription** in `useAuth` — `display_name`, `avatar_url`, `default_realm` etc. update live across browsers without a page reload."
        },
        {
          "label": "Fixed",
          "body": "- **`useChromeStorage.update` race condition.** The `valueRef`-based update path could be one render stale, letting rapid concurrent writes overwrite each other (the lingering \"Profile Name doesn't save on one of my browsers\" report). Replaced with functional `setValue(prev => …)` so `prev` is guaranteed to be the latest committed state. Universal fix — covers every callsite, not just the install identity."
        },
        {
          "label": "Migration",
          "body": "Run `supabase/migrations/011_add_profiles_to_realtime.sql` then `012_manager_scoping_and_invite_mint.sql` before relying on Team Activity or live profile updates."
        },
        {
          "label": "Out of scope (still parked for Phase D₂+)",
          "body": "- Desktop companion registers as a `browser_profiles` row (`browser='desktop_companion'`).\n- Mobile app(s) follow the same identity pattern.\n- Auto-update distribution via Chrome Web Store / signed CRX (Plan 019)."
        }
      ]
    },
    {
      "version": "5.1.1",
      "title": "Phase A race fix + classification explainer + invite-token note",
      "date": "2026-05-19",
      "intro": "",
      "sections": [
        {
          "label": "Fixed",
          "body": "- **Classification picker stuck on Professional / wouldn't save.** The eager-init useEffect's storage write was racing the user's first edit — `valueRef.current` could still be the stale DEFAULT, and the second write clobbered `localId` back to null. New self-healing `writeIdentity` ensures `localId` + `createdAt` exist on every write, so the race is impossible. Same fix covers profile-name edits."
        },
        {
          "label": "Added",
          "body": "- **Per-classification explainer** beneath the Classification picker, updates live with the selection. Covers what each of Business / Professional / Work / Personal *means* in terms of clock visibility, default realm, and cross-profile presentation.\n- **Per-option tooltips** on each Classification dropdown option.\n- **Invite Token clarification.** Added a small explainer above the Team Invite Token input noting that redemption works end-to-end but token *creation* doesn't have UI yet — admins generate tokens directly in the cloud console. The mint UI ships with the manager dashboard."
        }
      ]
    },
    {
      "version": "5.1.0",
      "title": "Multi-Profile Awareness Sync Phase C + Phase A polish",
      "date": "2026-05-19",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Cross-profile awareness substrate** (`tabatha.browser_profile_status`). Each install upserts its own row with clock state + active focus state on every transition; refreshes a `last_heartbeat_at` on a 60-second tick. Other installs subscribe via Supabase Realtime and render compact chips on the home dashboard (\"Personal 🏠 · 🎯 Slack · 6m\").\n- **`awarenessService.js`** — manages heartbeat, debounced state pushes, realtime subscription, and the `_otherProfiles` local cache.\n- **`OtherProfilesStrip` component** + **`useOtherProfiles` hook** — render the awareness chips with stale/offline dimming when no heartbeat in 5 minutes.\n- **Eager Install ID initialisation** in the `useInstallIdentity` React hook so the UI is responsive immediately, not blocked on the first sync.\n- **Save feedback** in Settings → Browser Profile: \"saving… / ✓ saved / save failed\" pill next to the section header.\n- **Immediate cloud push on Classification change** (no longer waiting for the next sync alarm)."
        },
        {
          "label": "Changed",
          "body": "- **Verbiage:** \"Supabase\" replaced with \"cloud\" / \"Account\" / \"database\" in user-facing copy. The settings sidebar entry \"☁️ Sync & Supabase\" is now \"☁️ Sync & Account\"; the re-pull dialog says \"re-pull from the cloud\"; the workshifts export stub says \"Sync to Cloud\"."
        },
        {
          "label": "Migration",
          "body": "Run `supabase/migrations/010_add_browser_profile_status.sql` before the cross-profile awareness chips will populate. Adds the status table, RLS policies, and adds the table to the `supabase_realtime` publication."
        },
        {
          "label": "Known limitations / out of scope",
          "body": "- The desktop companion does not yet register a `browser_profiles` row (`browser = 'desktop_companion'`); awareness chips for the companion will arrive in Phase D.\n- Clock-stacking warnings (when multiple non-personal profiles clock in simultaneously) are visualised by the chips but not yet inline-blocked. Deliberate — we want to observe stacking patterns before deciding the UX.\n- Manager-scoped views over org_members' profiles still rely on a future RLS expansion."
        }
      ]
    },
    {
      "version": "5.0.0",
      "title": "Multi-Profile Awareness Sync Phase A+B",
      "date": "2026-05-19",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Per-install browser-profile identity** stored locally in `_browserProfile` (chrome.storage.local). Each Chrome profile on each machine gets a stable `localId` on first run and a `supabaseId` on first sync.\n- **`tabatha.browser_profiles` is now active.** Every install upserts a row carrying its classification, profile name, and `last_seen_at`. The table has been present since migration 001; this release wires it up.\n- **Classification axis** (per-install): Business / Professional / Work / Personal. Distinct from the user-level `profiles.default_realm`. Picker lives in Settings → Appearance → \"This Browser Profile\".\n- **Bootstrap pull of org registry.** On first sync after sign-in, every `tabatha.{operations,initiatives,clients,projects,tasks_registry}` row for the user is fetched and merged into local `tabathaOrg` by name (case-insensitive). Prevents duplicate clients/projects/tasks when signing in on a second browser profile or machine.\n- **`↻ Re-pull registry`** button in Settings → Sync — clears the bootstrap watermark and re-runs the merge on demand.\n- **Stamp `browser_profile_id`** on every push: `focus_items`, `intent_history`, `clock_sessions`, `desktop_activity`, `operations`, `initiatives`, `clients`, `projects`, `tasks_registry`. The server can now attribute every row to the install that produced it.\n- **`useInstallIdentity()` React hook** exposes `{ identity, isPersonal, isReady, setClassification, setProfileName }` to UI surfaces."
        },
        {
          "label": "Changed",
          "body": "- **Personal-classified profiles hide clock-in / clock-out / break controls** in the home dashboard's Shift Controls section, the sidebar header, and the Command Palette. Time-and-attention breakdowns (UnifiedTimeline, AnalyticsDashboard, workshifts) remain visible everywhere.\n- **`applyDefaultRealm` (focusService)** now reads `_browserProfile.classification` first, falling back to the legacy `tabathaSettings.defaultRealm`. New focuses created on a Personal profile default to `realm=personal` independent of the user-level default.\n- Settings → Appearance → Browser Profile rebuilt into a \"This Browser Profile\" card: Install ID (last-12 of supabaseId), editable Profile Name, Classification picker."
        },
        {
          "label": "Migration",
          "body": "Run `supabase/migrations/009_add_browser_profile_stamp.sql` before relying on the new stamping behaviour. Until 009 is applied, push diagnostics will report column-missing errors for `browser_profile_id`."
        },
        {
          "label": "Required next steps after install",
          "body": "1. Set the **Classification** for this browser profile in Settings → Appearance.\n2. Hit **↻ Sync now** in Settings → Sync to register the install in `tabatha.browser_profiles`.\n3. When signing in on a second browser profile or machine, the bootstrap pull runs automatically on the first sync. Use **↻ Re-pull registry** to re-run it manually."
        },
        {
          "label": "Out of scope (deferred to next plan)",
          "body": "- Realtime awareness pings (cross-profile focus/clock chips).\n- Clock-stacking warnings when multiple non-personal profiles are clocked in.\n- Personal-profile \"see-all-my-activity\" aggregated dashboard.\n- Manager-scoped views over employees' browser profiles."
        }
      ]
    },
    {
      "version": "4.7.6",
      "title": "Supabase sync Batch 1 durable data coverage",
      "date": "2026-05-18",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- Added Supabase migration 008 for durable local data: operations, initiatives, clients, projects, task registry, clock sessions, and desktop activity.\n- Extended `syncService` to push `tabathaOrg`, completed focus history, `clockHistory`, `companionRecentSessions`, and `desktopActivity`.\n- Added watermarks for clock and desktop activity sync (`lastClockSync`, `lastDesktopActivitySync`) and storage-change sync triggers for direct page writes."
        },
        {
          "label": "Changed",
          "body": "- Focus sync now includes `focusEngine.history` as completed rows in `tabatha.focus_items`, not only active/queued items.\n- Clock-out now explicitly queues a Supabase sync in addition to the regular storage-change/alarm paths."
        },
        {
          "label": "Required Supabase migration",
          "body": "Run `supabase/migrations/008_add_batch1_sync_tables.sql` before relying on Batch 1 sync tables. Until migration 008 is applied, the Sync Status panel will report table-specific upsert failures."
        }
      ]
    },
    {
      "version": "4.3.0",
      "title": "Plan 025: Popup Harmony + Checkpoint Progress Notes + Context-Link",
      "date": "2026-05-17",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Singleton Popup Coordination** (#185): FTE/WBP overlays no longer stack across tabs. A `_activePopup` storage key tracks the active overlay; all other tabs skip rendering. Actions taken on any surface (home, sidebar, InBar) broadcast `POPUP_DISMISSED` to auto-clear stale overlays everywhere.\n- **Enhanced FTE CTAs**: Focus Timer Expired card expanded from 2 buttons to 6: ⏱️ Extend 5m, 🔄 Switch Focus (inline picker), ⏸ Pause, ☕ Step Away (break), ✅ Complete, 📝 Add Note.\n- **Combo Popup** (FTE + WBP): When the focus timer expires while the user is away, a single merged `FOCUS_RETURN_COMBO` card shows idle duration (with seconds) + all FTE CTAs + Resume. Replaces the previous two-popup stack.\n- **Configurable WBP Thresholds**: `welcomeBackMinIdleMinutes` (default 5) and `welcomeBackShowAfterBreak` (default true) in Settings → Follow-through Support.\n- **Off-Device Tag**: Boolean on focus items — when true, suppresses FTE/WBP/nudge popups and Chrome notifications. Toggle in home FocusBar and sidebar.\n- **Checkpoint Progress Notes** (#184): Timed auto-prompts at configurable intervals (`checkpointIntervalFraction`, default ⅓ of focus timer). Progress levels: `stuck` (0), `none` (0), `little` (1), `lot` (3), `almost_done` (4). Each CPN stored with `progressValue` for future scoring.\n- **CPN Snooze**: \"Remind me in 5 min\" on checkpoint prompt defers re-prompt.\n- **CPN Smart Suppression**: Auto-prompt skipped when a subtask/linked task was recently completed (only checked when subtasks exist).\n- **InBar Manual Checkpoint** (📋): Amber-highlighted button when no checkpoint logged in 30+ minutes. Opens inline CPN overlay with progress-level buttons.\n- **InBar Staleness Signal**: Pulsing amber dot on the checkpoint button when `lastCheckpointAt` exceeds `checkpointStaleMinutes` (default 30).\n- **Sidebar Checkpoint**: 📋 button in sidebar focus panel with inline textarea + progress-level buttons.\n- **Home CPN Timeline**: Collapsible checkpoint history in the FocusBar with timestamped entries.\n- **Follow-through Support Settings**: New settings section with toggles for checkpoint prompts, interval fraction slider, staleness threshold, WBP min idle, and Asana auto-post.\n- **Context-Link Indicator** (#186): InBar center label shows 🔗 (linked) or ⚡ (unlinked) icon based on whether the current tab is in `activeFocus.associatedTabIds`.\n- **Window Counts** (#186): Home FocusBar and sidebar focus card now display unique window count alongside tab count (e.g. \"3 tabs · 2 windows\").\n- **CPN → Follow-Through Heatmap**: ActivityHeatmap's \"Follow-Through\" view now counts both focus completions and checkpoint note submissions per day."
        },
        {
          "label": "Changed",
          "body": "- **`GET_INBAR_DATA` response** expanded with `isTabLinked` (boolean) and `windowCount` (computed via `chrome.tabs.get` across associated tabs).\n- **`focusService` alarm routing**: `checkpoint-prompt-{focusId}` alarm created on `startFocus()`, cleared on complete/pause/switch."
        },
        {
          "label": "Feature Docs",
          "body": "- `docs/features/184-checkpoint-progress-notes.md` — CPN data model, behavior spec, settings\n- `docs/features/185-popup-harmony.md` — Singleton coordination, combo popup, WBP thresholds\n- `docs/features/186-context-link-indicator.md` — Link indicator, window counts"
        }
      ]
    },
    {
      "version": "4.0.0",
      "title": "Cumulative release: v3.0 → v4.0 + Plan 023 service decomposition",
      "date": "2026-05-14",
      "intro": "This is the first cumulative release going to `staging` since `v3.0.0`. It carries\n**~35 intermediate development versions** (v3.12.4-α → v3.34.5) that lived on the\nintegration branch but never landed on `staging`, plus the Plan-023 background\nservice decomposition (v3.34.5 → v4.0.0). The version was bumped to MAJOR as a\nmarker for the magnitude of the drop, not because of any individual breaking\nchange in the ledger sense.\n\n> **Action required after install:** apply [`supabase/migrations/005_add_profile_defaults.sql`](supabase/migrations/005_add_profile_defaults.sql) in the Supabase SQL Editor. Without it, your profile select fails silently and Supabase sync never pushes any data. See **Required Supabase migration** below.",
      "sections": [
        {
          "label": "🚀 Added — Major features delivered in this window",
          "body": "- **Intent-to-Focus Bridge** (v3.34.5): typing an intent on a tab can auto-queue/auto-create a matching focus item, configurable via `intentBridgeMode` (`manual` / `smart_dedup` / `always`).\n- **Create Focus from Tab** + **Browser Profile Identity** (v3.34.5): each browser profile gets a stable ID stored in `chrome.storage.local.browserProfileId` for cross-device routing.\n- **Sidebar edit parity + InBar create focus** (v3.31.5): edit dropdown, refresh button, focus queue mutations from sidebar match home parity.\n- **Project / Client tag editing** (v3.30.5): live edit of focus tags (`realm`, `client`, `project`, `task`) on the active focus.\n- **Activity Editor + Timeline breaks + Webhook intervals** (v3.29.5): per-entry trim/split/merge, manual break insertion on the timeline, configurable webhook firing cadence.\n- **Desktop Activity editor** (v3.25.2): trim/clear tools for desktop activity captured via companion bridge.\n- **Sidebar intent creation with timer parity** (v3.24.2): new-focus input now lives in sidebar too.\n- **Timeline today-filter** (v3.23.2): desktop activity timeline scoped to current day by default.\n- **InBar layout overhaul + URL pause matching** (v3.21.0): paused-intent state survives URL changes when the new URL still matches the pattern.\n- **Task storage migration + funnel stage state machine** (v3.20.0): legacy `tasks[]` array → structured `tabathaOrg.tasks` registry. Funnel stages canonicalized: `unsorted → todo → focus → addressing → resolved → roadblocked`. One-time, flag-gated migration runs once per profile.\n- **Multi-task picker + auto-fill cascade + business attribution + task CRUD** (Tier-3): hierarchy-aware task selection, parent → child tag inheritance.\n- **Org hierarchy + stage editing + resolution tracking** (Tier-3): full org-tree view with stage editor.\n- **Initiatives panel** (Tier-3): roll-up of focuses by initiative across the org tree.\n- **AnalyticsDashboard** (post-3.12.4): 5 stat cards (focuses today, focus time, completion rate, streak, open tasks), top-focuses bar chart, category-time breakdown, context distribution.\n- **ActivityHeatmap** ×3 (v3.12.4-α): GitHub-style 365-day contribution graph with three views — Browser, Overall, Follow-Through.\n- **ProjectsClientsPanel + InitiativesPanel** (v3.12.4-α): full client/project view.\n- **KeyboardShortcuts + VoiceInput** (v3.12.4-α): expanded keyboard chords and voice intent capture.\n- **InBar Edit Dropdown** (v3.12.4-α): ✏️ inline panel for intent edit, focus assignment, new focus creation.\n- **InBar Intent/Focus Split** (v3.12.4-α): tab intent and central focus shown separately with divider.\n- **Focus Pause/Resume + Focus Edit + Side-Quest Auto-Pause** (v3.12.4-α): paused focuses move to queue with amber styling; resume reactivates timer.\n- **Auto-Park paused tabs on close** (v3.12.4-α): paused tabs auto-park with their sticky-note preserved.\n- **Tab label editing + Link Tab to Intent** (v3.12.4-α): inline rename, tab-picker dropdown for intent linking.\n- **Collapsible homepage sections** (v3.12.4-α): persisted collapse state for every section.\n- **Data Retention Alarm** (v3.12.4-α): daily prune of companion/desktop activity older than configurable threshold (default 90d).\n- **LogsPanel overhaul** (v3.12.4-α): 8 log types with toggleable filter chips and pagination (50/load)."
        },
        {
          "label": "🆕 Added — v4.0.0 quality-of-life",
          "body": "- **Editable display name** in Settings → Account: click your name to edit. Writes through to `tabatha.profiles.display_name`.\n- **Manual `Export Markdown` button** in Settings → Export & Agents: downloads a snapshot of active tabs, contexts, closed sessions, and time tracking on demand. The auto-export alarm continues to work as before.\n- **Sync Status panel** in Settings → Account: shows last successful sync time and a recent-events log of any sync diagnostics (no profile row, missing columns, upsert errors, etc.). Surfaces failures without needing the Service Worker DevTools console."
        },
        {
          "label": "🔧 Internal — Plan 023 service decomposition",
          "body": "- **Background service decomposition**: `background.js` collapsed from 2,920 lines (staging) → 169 lines (orchestrator only). Runtime message routing, listener registration, and alarm dispatch now live in dedicated services: `tabService`, `focusService`, `taskService`, `clockService`, `clockTickService`, `tabTrackingService`, `categoryService`, `sessionService`, `notificationService`, `settingsService`, `groupService`, `blockgateService`, `companionService`, `alarmService`, `syncService`.\n- **Alarm consolidation**: three `chrome.alarms.onAlarm` listeners merged into a single dispatcher in `alarmService`; `supabase-sync` is auth-guarded before dispatch (skipped when no Supabase session, no longer enters `syncToSupabase` and bails inside).\n- **Storage caps**: `intentHistory`, `closedContexts`, `sessions`, `sugarBox`, and `focusEngine.history` archived through `archiveBeforeCap` instead of being silently truncated.\n- **Settings → Storage** block: `settings.storage.*` with additive migration. New tunables: `sugarBoxCap`, `snapshotIntervalMinutes`, `archivedTasksColdAfterDays`, `parkedTabsWarnAt`, `pendingTimeLogsWarnAt`, `focusHistoryCap`, `closedContextsCap`, `intentHistoryCap`.\n- **Companion reconnect** is now exponential-backoff capped at 30s instead of constant fast retry.\n- **`COMPANION_IDLE_STATE`**: transitional broadcast — companion-detected idle is now mirrored to all extension pages.\n- **`clockTickService`**: shared 1Hz tick broadcaster (`TICK_SUBSCRIBE`, `TICK_UNSUBSCRIBE`, `GET_TICK_STATUS`) so extension pages can stop running per-component intervals.\n- **`PARKED_TABS_WARNING` broadcast**: one-shot when parked tabs hit `settings.storage.parkedTabsWarnAt`.\n- **`STORAGE_CAP_WARNING` broadcast**: emitted when sugarBox entries fall off the cap."
        },
        {
          "label": "🐛 Fixed — Critical pre-Plan-023 fixes",
          "body": "- **Critical: data loss + break/focus sync + funnel reorder**: prior to v3.13 a race in clock+focus state machines could discard active-focus state on break end.\n- **Critical: TDZ + API consistency**: temporal dead-zone errors during boot caused unpredictable startup state.\n- **InBar `SET_INTENT` was silently dropped** (v3.22.1): handler was missing entirely from the background router until this fix.\n- **Drifted-focus timer extension preserved elapsed time** (v3.22.1): previously reset to zero on extend.\n- **Duplicate notification listeners** (v0.2.5-α): merged into single handler; eliminated service-worker unpredictability.\n- **`activeTabId` ReferenceError** in welcome-back notification — replaced with `WINDOW_ID_CURRENT`.\n- **`triggerSync` excessive firing** (v0.2.5-α): auth-session guard added.\n- **`useChromeStorage` stale closure** (v0.2.5-α): `update` callback now uses `useRef` to avoid capturing stale `value`.\n- **`patternToRegex` double-escape** (v0.2.5-α): rewrote to split on `*` first, escape segments individually.\n- **Status normalization** (v3.17.24): inconsistent display of `complete` vs `completed` across UI surfaces.\n- Stage pills, palette nav, task delete, InPop nesting, stage-state sync, paused→resolved transitions, T2.1/T2.3/T4.2 surface fixes (v3.17.13 → v3.17.24): cluster of post-merge user-testing fixes.\n- **`logEvent` was used before it was defined** in background.js — defined.\n- **Timeline tooltip clipping** (v3.0.0-α): portal'd to `document.body` to escape backdrop-filter containing block; max z-index applied.\n- **Corner radius / InBar label fallback / responsive FlipClock / task delete confirm**: UI polish."
        },
        {
          "label": "🐛 Fixed — v4.0.0 cleanup pass (this release)",
          "body": "- **Completed-focus counter never incremented**: AnalyticsDashboard read `intentHistory` for resolved counts, but `completeFocus()` moves items into `engine.history`. Two storage keys, never crossed. The dashboard now sources resolved focuses from `focusHistory` directly. ([src/home/AnalyticsDashboard.jsx](src/home/AnalyticsDashboard.jsx))\n- **Streak counted only queue items**: extended to include `engine.history` and the currently-active focus, so a day where you only completed focuses still counts in the streak. ([src/home/AnalyticsDashboard.jsx](src/home/AnalyticsDashboard.jsx))\n- **Supabase sync silently bailed forever**: `tabatha.profiles` was missing the `default_org_id` and `default_team_id` columns that both `useAuth.fetchProfile` and `syncService.syncToSupabase` were selecting. The error was thrown away, sync logged \"No profile found for user\" misleadingly, and nothing ever made it to Supabase. Fixed by:\n  - New [`supabase/migrations/005_add_profile_defaults.sql`](supabase/migrations/005_add_profile_defaults.sql) adds both columns (nullable, FK to organizations/teams).\n  - `syncService` and `useAuth` now use `maybeSingle()`, capture the error, and fall back to a minimal `SELECT id` so the rest of the flow still works pre-migration.\n  - Every silent bail now writes a row to `chrome.storage.local._syncDiagnostics`, surfaced in Settings → Account.\n- **Display name stuck at \"Tabatha User\"**: until now there was no UI to change it. Profile rows auto-provisioned without an OAuth `full_name` (e.g. from magic-link or password sign-up) had no way to be fixed. Added inline editor in Settings → Account.\n- **No on-demand markdown export**: the `EXPORT_MARKDOWN` handler existed in `sessionService` but nothing in the UI called it. Auto-export still worked. Added a button in Settings → Export & Agents.\n- **Transitional `serviceFlags.focus.ready` stub removed** from `tabService`: PR #11 left a dormant feature flag and dead fallback bodies for `autoQueueFromIntent` / `linkTabToFocus`. Both call sites now delegate directly through the injected `focusService` deps."
        },
        {
          "label": "⚠️ Schema notes",
          "body": "- **`intentChangeLog` removed** — merged into `intentHistory` with a union shape (one-time, flag-gated migration via `_intentLogMigrated`). External readers must read `intentHistory`.\n- **Legacy `tasks[]` migrated** to `tabathaOrg.tasks` registry (one-time, flag-gated via `_tasksMigrated`). The legacy `tasks` key is set to `[]` after migration; a snapshot is briefly stored in `_legacyTasksBackup` then removed by tabService's one-shot cleanup.\n- **Archived tasks** older than `settings.storage.archivedTasksColdAfterDays` (default 90d) move from `tabathaOrg.tasks` to `_archivedTasks` (internal cold-store key).\n- **`settings.storage` block** added with additive migration — user-tuned values preserved, missing fields back-filled from `DEFAULT_SETTINGS.storage`."
        },
        {
          "label": "🛠 Required Supabase migration",
          "body": "Run [`supabase/migrations/005_add_profile_defaults.sql`](supabase/migrations/005_add_profile_defaults.sql) in the Supabase SQL Editor:\n\n```sql\nALTER TABLE tabatha.profiles\n  ADD COLUMN IF NOT EXISTS default_org_id UUID REFERENCES tabatha.organizations(id) ON DELETE SET NULL;\nALTER TABLE tabatha.profiles\n  ADD COLUMN IF NOT EXISTS default_team_id UUID REFERENCES tabatha.teams(id) ON DELETE SET NULL;\nCREATE INDEX IF NOT EXISTS profiles_default_org_idx ON tabatha.profiles(default_org_id);\nCREATE INDEX IF NOT EXISTS profiles_default_team_idx ON tabatha.profiles(default_team_id);\n```\n\nThe extension boots and operates without this migration (sync falls back to a minimal profile select that omits org/team scoping), but no rows reach `focus_items` / `intent_history` until the columns exist."
        },
        {
          "label": "📦 Upgrade procedure (existing v3.34.5 unpacked install)",
          "body": "Data is preserved by chrome.storage migrations — `intentChangeLog`, legacy tasks, and additive settings backfill all run flag-gated and idempotently. The key thing is to upgrade **in-place** so Chrome keeps the same extension ID:\n\n1. Back up your data first (open any Tabatha page DevTools console → `chrome.storage.local.get(null, d => copy(JSON.stringify(d)))` → paste to a `.json` file).\n2. Apply the Supabase migration above.\n3. Overwrite the contents of your existing `dist/` directory with the new v4.0.0 build (do NOT load v4.0.0 from a new path — that creates a second extension ID with empty storage).\n4. In `chrome://extensions`, hit \"Reload\" on the same Tabatha extension card.\n5. Open the Service Worker (or any Tabatha page) once so bootstrap migrations fire.\n6. Settings → Account → confirm `Sync Status` shows a recent successful sync."
        },
        {
          "label": "🔁 Known limitations (not blockers, planned follow-up)",
          "body": "- Supabase sync is currently one-directional (local → cloud). Tabs/focuses created on Browser A don't auto-pull to Browser B.\n- `tabathaOrg` (clients/projects/tasks/operations/initiatives) is local-only; not synced to Supabase yet.\n- Tab groups created via Chrome's built-in UI are tracked by Tabatha's `groupService` but not surfaced in any Tabatha panel — no per-group view yet.\n- Companion bridge has no profile scoping; running Tabatha in two browser profiles simultaneously will cause both to share companion events. Stop the companion app before regression-testing in a second profile."
        }
      ]
    },
    {
      "version": "3.34.5",
      "title": "Intent-to-Focus Bridge + Browser Profile Identity",
      "date": "2026-05-12",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Intent-to-Focus Bridge**: typing an intent on a tab can auto-queue/auto-create a matching focus item, configurable via `intentBridgeMode` (`manual` / `smart_dedup` / `always`).\n- **Create Focus from Tab**: new-focus flow accessible directly from tab cards, pre-filled with the tab's URL and title.\n- **Browser Profile Identity**: each Chrome profile gets a stable `browserProfileId` stored in `chrome.storage.local` for future cross-device routing.\n\n`fc9882c`"
        }
      ]
    },
    {
      "version": "3.31.5",
      "title": "Sidebar Edit Parity + InBar Create Focus",
      "date": "2026-05-12",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Sidebar edit dropdown**: full intent/focus editing from sidebar matches home parity.\n- **InBar create focus**: create a new focus item directly from the content bar.\n- **Refresh button**: manual data refresh in sidebar header.\n\n`8cd185e`"
        }
      ]
    },
    {
      "version": "3.30.5",
      "title": "Focus Tag Editing",
      "date": "2026-05-12",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Project/Client tag editing** on active focus: live edit of `realm`, `client`, `project`, and `task` tags from the FocusBar edit panel.\n\n`6a70047`"
        }
      ]
    },
    {
      "version": "3.29.5",
      "title": "Activity Editor + Webhooks",
      "date": "2026-05-12",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Activity Editor**: per-entry trim/split/merge on timeline entries.\n- **Timeline break insertion**: manual break markers on the activity timeline.\n- **InBar sync indicator**: visual feedback when InBar data is stale.\n- **Webhook intervals**: configurable firing cadence for webhook integrations.\n\n`481f0fb`"
        }
      ]
    },
    {
      "version": "3.25.2",
      "title": "Desktop Activity Editor",
      "date": "2026-05-12",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Desktop Activity editor** in Settings: trim and clear tools for desktop activity captured via companion bridge.\n\n`f4dd6b3`"
        }
      ]
    },
    {
      "version": "3.24.2",
      "title": "Sidebar Intent Creation",
      "date": "2026-05-12",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Sidebar intent creation** with full timer parity — new-focus input now lives in the sidebar alongside home.\n\n`942524e`"
        }
      ]
    },
    {
      "version": "3.23.2",
      "title": "Timeline Today Filter",
      "date": "2026-05-12",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Desktop activity timeline scoped to current day** by default."
        },
        {
          "label": "Fixed",
          "body": "- **InBar `SET_INTENT` was silently dropped** — handler was missing entirely from the background router. (`a91f361`)\n\n`a0370d1`"
        }
      ]
    },
    {
      "version": "3.22.1",
      "title": "Timer Fix + Version Sync",
      "date": "2026-05-12",
      "intro": "",
      "sections": [
        {
          "label": "Fixed",
          "body": "- **Drifted-focus timer extension** now preserves elapsed time instead of resetting to zero on extend. (`9f7fbf8`)\n\n`9b09fcc`"
        }
      ]
    },
    {
      "version": "3.21.0",
      "title": "InBar Layout Overhaul",
      "date": "2026-05-12",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **InBar layout overhaul**: restructured content bar with intent lifecycle display.\n- **URL pause matching**: paused-intent state survives URL changes when the new URL still matches the pattern.\n\n`34f0371`"
        }
      ]
    },
    {
      "version": "3.20.0",
      "title": "Task Migration + Funnel State Machine",
      "date": "2026-05-12",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Task storage migration**: legacy `tasks[]` array → structured `tabathaOrg.tasks` registry. One-time, flag-gated migration runs once per profile.\n- **Funnel stage state machine**: stages canonicalized as `unsorted → todo → focus → addressing → resolved → roadblocked` with transition rules.\n- **Task stage management** in sidebar with stage picker.\n- **Sidebar pause** for active focuses."
        },
        {
          "label": "Schema",
          "body": "- New storage key: `tabathaOrg.tasks` (migrated from `tasks[]`)\n- Migration flag: `_tasksMigrated`\n- Backup key: `_legacyTasksBackup` (removed after one-shot cleanup)\n\n`0c40b1c`, `185ef9e`"
        }
      ]
    },
    {
      "version": "3.17.24",
      "title": "Status Normalization",
      "date": "2026-05-12",
      "intro": "",
      "sections": [
        {
          "label": "Fixed",
          "body": "- **Status normalization**: inconsistent display of `complete` vs `completed` across UI surfaces resolved with canonical status mapping.\n- Display precedence fixes for funnel stage vs focus state.\n\n`2f7147e`"
        }
      ]
    },
    {
      "version": "3.17.22",
      "title": "Palette + InPop Fixes",
      "date": "2026-05-12",
      "intro": "",
      "sections": [
        {
          "label": "Fixed",
          "body": "- **Palette navigation** edge cases.\n- **Task delete** confirmation missing for some paths.\n- **InPop nesting** — nested overlays no longer stack incorrectly.\n- **Stage-state sync** between sidebar and home views.\n\n`b00de1d`"
        }
      ]
    },
    {
      "version": "3.17.18",
      "title": "Stage Pills + Paused State",
      "date": "2026-05-12",
      "intro": "",
      "sections": [
        {
          "label": "Fixed",
          "body": "- **Stage pills** rendering consistency.\n- **Task-per-char** input regression (character-by-character task creation).\n- **Palette break** handling edge case.\n- **Paused → Resolved** transition now requires confirmation.\n\n`d2cecac`"
        }
      ]
    },
    {
      "version": "3.17.14",
      "title": "Tier 2/4 Surface Fixes",
      "date": "2026-05-11",
      "intro": "",
      "sections": [
        {
          "label": "Fixed",
          "body": "- **T2.1** stage pills alignment and color mapping.\n- **T2.3** task status not reflecting actual completion state.\n- **T4.2** Command Palette selection not applying.\n\n`8cc6649`"
        }
      ]
    },
    {
      "version": "3.17.13",
      "title": "Post-Merge Bug Fix Sweep",
      "date": "2026-05-11",
      "intro": "",
      "sections": [
        {
          "label": "Fixed",
          "body": "- 8 bug fixes from live user testing feedback covering focus resolution, timer display, intent association, and sidebar rendering.\n\n`eae1e86`"
        }
      ]
    },
    {
      "version": "3.12.4-alpha",
      "title": "InBar, Focus Controls, Heatmap, Logs & Activity Overhaul",
      "date": "2026-05-11",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **InBar Edit Dropdown**: ✏️ button opens inline panel for intent editing, focus assignment, and new focus creation — all from the content bar.\n- **InBar Intent/Focus Split**: Bar now shows tab intent and central focus separately with a visual divider.\n- **Focus Pause/Resume**: ⏸ button on FocusBar freezes timer, moves focus to queue with amber styling. FocusInput reappears for new focus.\n- **Focus Edit**: ✏️ button on FocusBar for inline rename, timer adjust, and funnel stage changes.\n- **Side-Quest Auto-Pause**: Starting a side quest automatically pauses the current active focus and resumes it when the side quest ends.\n- **Auto-Park on Close**: Paused tabs auto-park with their sticky note preserved when closed.\n- **Tab Label Editing**: ✏️ rename button on each tab card in the Tabs panel. Custom titles persist with original shown on hover.\n- **Link Tab to Intent**: 📄 Link Tab button in IntentsPanel with inline open-tab picker dropdown.\n- **Collapsible Sections**: All homepage sections collapsible with persisted state (Shift Controls, Now Bar, Focus Engine, Activity, Nav Tabs).\n- **Activity Heatmaps (×3)**: GitHub-style contribution graph with 3 views — Browser, Overall, Follow-Through. Theme-aware colors, hover tooltips, 365-day range.\n- **Context Activity Bar**: Renamed from \"Desktop Activity\" to include browser + desktop + mobile (future) segments.\n- **Data Retention Alarm**: Daily chrome.alarm prunes companion/desktop activity older than configurable threshold (default 90 days).\n- **Data Retention Setting**: Configurable in Settings → Time Tracking with description text.\n- **Parked Tab Notes**: Parked tabs display context badge, auto-park source indicator, and preserved sticky notes."
        },
        {
          "label": "Changed",
          "body": "- **LogsPanel Overhaul**: Now supports 8 log types (Tab Activity, Intent Change, Focus Session, Clock Stint, Break, Context Set, Blocked Site, Task Update) with toggleable filter chips and pagination (50 per load). Desktop activity excluded — reserved for Context Activity Bar.\n- **Header Spacing**: Reduced padding above/below header. Clock wrapper fixed-height prevents layout shift at different scales.\n- **Version**: Bumped to 3.12.4-alpha across manifest.json, settings, and homepage."
        }
      ]
    },
    {
      "version": "0.2.5-alpha",
      "title": "Diagnostic Fix Sweep",
      "date": "2026-05-09",
      "intro": "",
      "sections": [
        {
          "label": "Fixed",
          "body": "- **Critical: Duplicate notification listeners** merged into single handler; eliminated service worker unpredictability.\n- **Critical: `activeTabId` ReferenceError** in welcome-back notification handler — replaced with `WINDOW_ID_CURRENT`.\n- **Critical: `export` keyword on `triggerSync()`** removed — prevented potential service worker module loading failure.\n- **Clock-In/Out race condition** — eliminated double-writes to `clockSession` storage key; UI now relies on reactive `useChromeStorage` listener.\n- **Focus actions fragility** — `completeFocus()` and `extendTimer()` now receive explicit `focusId` in home and sidebar.\n- **Time tracking shows 0s** — added `updateTimeTrackingAggregates()` to bridge `pendingTimeLogs` and the `timeTracking.byTab` storage key the UI reads.\n- **Gatekeeper Sugar Box/Park/Later** — buttons now close overlay and tab as tooltips promised.\n- **`useChromeStorage` stale closure** — `update` callback uses `useRef` to avoid capturing stale `value`.\n- **Popup `new URL()` crash** — wrapped in try/catch for `chrome://` and malformed URLs.\n- **`triggerSync` excessive firing** — added auth session guard to skip Supabase calls when unauthenticated.\n- **`patternToRegex` double-escape** — rewrote to split on `*` first, escape segments individually."
        },
        {
          "label": "Changed",
          "body": "- **Shared `formatTime` utility** — extracted from 3 duplicate definitions into `src/utils/formatTime.js`."
        }
      ]
    },
    {
      "version": "0.2.4-alpha",
      "title": "Phase 3/4 Refinements",
      "date": "2026-04-28",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Logs Panel**: Replaced simple Time view with deep filtering (Date, Intent, Category, Duration) for historical activity tracking.\n- **Link/Merge Modal**: Universal modal to link Tabs to Intents, or merge Intents into Tasks.\n- **Settings Walkthrough**: In-app educational tooltips describing \"When\", \"How\", and \"Affects\" for all configuration options.\n- **Theme Expansion**: High contrast corporate theme, plus 5 new distinct themes (Neo-Brutalism, Glass Ocean, Retro Pixel, Solarized Warm, High Contrast Dark).\n- **Background Handlers**: Automated logic for `CLOCK_IN`, `CLOCK_OUT`, and `TOGGLE_BREAK`."
        },
        {
          "label": "Changed",
          "body": "- **Gatekeeper Parked Tabs**: Automatically restores session context for parked tabs, preventing redundant Gatekeeper prompts.\n- **Dashboard Refinement**: Renamed \"Contexts\" to \"Sessions\" and added \"Link/Close\" actions to tab listings."
        }
      ]
    },
    {
      "version": "0.1.5",
      "title": "Phase 1.5 (User Enhancements)",
      "date": "2026-02-12",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Gatekeeper Overlay**: A new interception mechanism for empty tabs. Instead\n  of a redirect or modal, a dark, immersive overlay appears on new tabs asking\n  for context/intent.\n  - Options: \"Continue\" (set context), \"Side Quest\" (5m timer), \"Sugar Box\"\n    (save for later), \"Park\" (save for later).\n- **Quick Access**: \"Speed Dial\" on the Welcome Page. Clicking a top site\n  immediately launches it with the context typed in the \"New Session Intent\"\n  box, bypassing the Gatekeeper.\n- **Welcome Page Parity**: The \"New Tab\" page (`home.html`) is now a\n  full-featured dashboard mirroring the Sidebar.\n  - Features: Tab list, Context view, Groups management, Time tracking, Restore\n    Session.\n  - Design: \"Mission Control\" desktop layout with wider UI elements.\n- **Time Tracking Logic**:\n  - Added \"Active Time\" tracking per tab (persists across sessions).\n  - Added \"Open Duration\" display.\n  - Added Pomodoro timer constraints.\n- **Sugar Box & Parked Tabs**: Stub storage implementations for saving\n  distractions (Sugar Box) and keeping tabs for later (Parked)."
        },
        {
          "label": "Changed",
          "body": "- **Welcome Page UI**: Completely overhauled `home.html` and `home.css` to use a\n  glassmorphism \"Mission Control\" aesthetic, sharing styles with the Sidebar.\n- **Tab Restore**: Improved \"Return to Flow\" logic to better handle session\n  restoration with priorities.\n- **Manifest**: Added `topSites` permission for Quick Access feature."
        },
        {
          "label": "Fixed",
          "body": "- **Empty Sidebar Bug**: Fixed an issue where the sidebar would be empty on\n  extension reload because existing tabs weren't re-synced to storage.\n- **Layout Issues**: fixed CSS conflicts between `sidebar.css` and `home.css`."
        }
      ]
    },
    {
      "version": "0.1.0",
      "title": "Phase 1 (Core Foundation)",
      "date": "2026-02-10",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Extension Scaffold**: MV3 Manifest, Service Worker (`background.js`), Side\n  Panel, Content Scripts.\n- **Context Engine**:\n  - Data structure for Tabs, Contexts, Intents, and Priorities.\n  - Logic for inheriting context from parent tabs.\n- **Sidebar UI**: Rich, interactive sidebar replacing the native vertical tabs.\n  - Sections: Intent Dashboard, Tab List, Batch Updates, Groups, Time Tracking.\n- **Tab Analysis**:\n  - **Categories**: Auto-detection of \"Work\", \"Media\", \"Social\", etc. based on\n    URL.\n  - **Priority System**: Critical (Red), High (Orange), Medium (Yellow), Low\n    (Green).\n- **Tab Groups**:\n  - Integration with native Chrome Tab Groups.\n  - **Sub-Groups**: \"Project\" layer above Chrome groups.\n- **Tab Locking**:\n  - **Lock**: Prevent accidental close.\n  - **URL Lock**: Prevent navigation away from a specific domain (e.g., lock a\n    tab to Asana).\n- **Markdown Export**: Auto-generates `Tabatha/context.md` for AI agents to\n  understand user context.\n- **Idle Detection**: Detects when user leaves Chrome and asks for off-screen\n  context upon return."
        },
        {
          "label": "Core Philosophy Implementation",
          "body": "- Established the \"Context-First\" data model where every tab must have a purpose\n  or inherit one."
        }
      ]
    },
    {
      "version": "Tabby Sidecar v0.1.0",
      "title": "Full sidebar parity + PWA + unified clock",
      "date": "2026-07-17",
      "intro": "Companion mobile web app (separate from the extension version line). Live at\nhttps://tabatha.pondocean.co/sidecar",
      "sections": [
        {
          "label": "Added",
          "body": "- **Near 1:1 Focus parity with the sidebar:** edit panel (label, timer, stage,\n  client/project, backdate start), checkpoint notes + timeline (new synced\n  `focus_checkpoints` table, migration 032), sub-intents, backburner dock\n  (resume/snooze/dismiss), on/off-computer toggle, NOW/drift states.\n- **Phone Focus Mode** — Page Visibility detection: leave the Sidecar and it\n  nudges you back (groundwork for phone-down focus triggers).\n- **PWA install** — web manifest + icons + Apple meta, so it installs to the\n  Home Screen (also what unlocks iOS Web Push).\n- **Expanded push parity** — `send-focus-push` now delivers timer-expiry,\n  **drift**, and **checkpoint-staleness** notifications (was timer only)."
        },
        {
          "label": "Changed",
          "body": "- **Clock** reframed from \"this phone's shift\" to **\"Your shift\"**, and now\n  surfaces other devices on the clock (\"Also on the clock\") — one shift, many\n  devices.\n- Renamed **off-device → off-computer** everywhere (clearer: at vs away from the\n  computer). Create CTA dropped the \"(off-device)\" tag (it's implied by surface).\n- **Pausing a focus keeps it pinned at the top** (locally-tracked current focus)\n  instead of demoting it into the queue."
        },
        {
          "label": "Fixed",
          "body": "- `browser_profiles` device registration upserted on a partial index\n  `ON CONFLICT` can't target → switched to the full `(profile_id, local_id)` key."
        },
        {
          "label": "Notes",
          "body": "- Round-trip stays account-synced (desktop reflects on its next pull). Checkpoints,\n  sub-intents, and backburner are Sidecar-side today (the extension doesn't sync\n  those fields yet) — full desktop round-trip is a follow-up extension change."
        }
      ]
    },
    {
      "version": "Tabby Sidecar v0.2.0",
      "title": "Landscape Context View + realtime",
      "date": "2026-07-17",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Landscape view-only Context View** — on a large landscape viewport\n  (computer / tablet / TV / 3rd screen) `/sidecar` becomes an ambient, view-only\n  focus screen: brand bottom-left, **day countdown (1440 min, tied to a new\n  `dayResetHour` setting)** top-right, current time bottom-middle, one giant\n  focus + timer + up-next. Controls stay on phone/extension. Auto-switches;\n  \"📺 Context view\" / \"Use controls →\" toggles.\n- **Realtime** — migration 033 adds `focus_items` + `browser_profile_status` to\n  the `supabase_realtime` publication; the app subscribes (verified SUBSCRIBED),\n  so the Context View updates live (poll kept as fallback).\n- Settings: **Day resets at (hr)** field driving the 1440 countdown."
        },
        {
          "label": "Fixed",
          "body": "- **Focus timer no longer restarts on resume** — accumulated elapsed is frozen on\n  pause (`tags._elapsedMs`) and the start is shifted on resume/switch so it\n  continues; a paused focus shows a frozen \"remaining\"."
        }
      ]
    },
    {
      "version": "Tabby Sidecar v0.2.1",
      "title": "Phone-away accountability on the big screen",
      "date": "2026-07-18",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **Phone-away red alert on the Context View.** When the phone is in **Phone\n  Focus Mode** and you navigate away from the Sidecar, the phone broadcasts an\n  \"away\" signal (`browser_profile_status.metadata.focusAway`); the big-screen\n  Context View — already subscribed via realtime — washes **red** with \"Put the\n  phone down\". **Slow fade-in (~7s) by default**, or **immediate** via a new\n  Settings toggle (\"Immediate phone-away alert\"). Clears when the phone returns.\n- Verified cross-device end-to-end under RLS (signal set → detected → cleared)."
        }
      ]
    },
    {
      "version": "Tabby Sidecar v0.3.0",
      "title": "Voice, chaperone v0, feedback, pause-on-leave",
      "date": "2026-07-18",
      "intro": "Built by the persona fleet under CeeCee (Plan 040 wave 1): Cindra (Lane A),\nCirra (Epic 1+10), Rook (Epic 7); Koda vetted; Kael ran the repo restoration.",
      "sections": [
        {
          "label": "Added",
          "body": "- **Voice capture (#165)** — mic on the new-intent input + checkpoint note\n  field (Web Speech, Android/desktop Chrome; graceful no-op on iOS).\n- **Chaperone v0 (#182)** — pre-recorded personality lines (\"Don't you dare\n  touch that phone.\") play on the Context View when the phone walks off in\n  Focus Mode; 'classic' pack, quiet hours, default off (settings.chaperone).\n- **Feedback & bug reporting (Epic 7)** — Settings card → feedback-to-asana\n  edge fn (now deployed, CORS widened to the sidecar origin) with an\n  AsyncStorage queue fallback; sidecar-only users can report.\n- **Context View: checkpoint counter + last-note preview** (optional,\n  settings.sidecar.showCheckpoints, default on).\n- **Empty-state choose-from cards** — no active focus ⇒ priority-ordered\n  pending-focus cards (view-only).\n- **focus_away server push** — reliable \"you walked off\" notification with\n  per-episode dedup (metadata.awayNotifiedAt vs awaySince)."
        },
        {
          "label": "Changed / Fixed",
          "body": "- **Pause-on-leave (B1):** leaving in Focus Mode pauses (not clears) the\n  active focus client-side, reusing the _elapsedMs freeze.\n- **Current focus is data-driven (B2):** active → most-recent paused\n  (non-resolved) → empty only when resolved; local pin is a tiebreaker."
        },
        {
          "label": "Repo/infra (extension line)",
          "body": "- GitHub restoration (Kael): staging reconciled at **6.7.27** (PRs #27–#31),\n  the 6.7.9→6.7.24 run pushed, dist defused to **6.7.24** (was unreviewed\n  6.8.2), 11 branches + 2 stale worktrees pruned (uncommitted InBar work\n  rescue-stashed). main untouched."
        }
      ]
    },
    {
      "version": "Tabby Sidecar v0.4.0",
      "title": "Context View timeline + layout v2 + focus_events",
      "date": "2026-07-18",
      "intro": "",
      "sections": [
        {
          "label": "Added",
          "body": "- **focus_events** log (migration 034, realtime) — every start/pause/resume/\n  resolve recorded; foundation for accurate per-focus/per-task time.\n- **Context View bottom timeline** — fills as the timer runs; nodes for\n  checkpoints + every start (press/hover = date-time + cumulative\n  \"📱 Sidecar-tracked\" time); overtime feeds a slow-pulsing, capped-growth\n  circle with a halo trail; reduced-motion respected.\n- **ProgressRing** — RN-native circular timer ring (no CSS conic-gradient),\n  native-build safe.\n- **Layout v2** — huge title top-left overlapping the ring, huge timer\n  bottom-right, brand+controls bottom-left, day-countdown compact top-right,\n  clock bottom-middle.\n- (v0.3.1) **Notes-simple capture mode** (\"What's on your mind?\" — type or\n  speak → intent) + **Install Tabby** PWA CTA."
        }
      ]
    }
  ]
}
