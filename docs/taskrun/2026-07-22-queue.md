# 2026-07-22 Overnight TaskRun Queue

> Assembled by Rook, dispatched by CeeCee. Sources: `docs/audits/2026-07-21-SYNTHESIS.md` (Koda),
> Malkio's 2026-07-22 fresh asks (a)-(e), remaining SYNTHESIS items 3-12, and open reconciliation
> chores discovered during assembly. Asana tracking: task `1216785460049994` ("Assemble 2026-07-22
> overnight TaskRun queue"), project `1214031898449333` (Flux Development).
>
> **Already shipped, not in this queue:** SYNTHESIS #1 (XSS escaping sweep across gatekeeper.js/inbar.js)
> and #2 (RLS state-aware `WITH CHECK` on `browser_profiles.revoked_at`) — confirmed live on `staging`
> HEAD `211a398`, "release(enterprise): 6.7.54 -> 6.7.56 (XSS escaping sweep + revoked_at DB guard)".
>
> **Ordering:** security/data-integrity → user-visible bugs → team-visibility features → hygiene.
> Items needing Malkio's judgment are pulled into the **Excluded / Morning Questions** section at the
> bottom, not left in the executable queue.

---

## Tier 1 — Security / data-integrity

### TR-01. Regina device-row cleanup (one-off server-side dedup)
- **Source:** Malkio ask (c), 2026-07-22.
- **What:** Regina's `browser_profiles` rows have the same pre-6.7.46 flood-dupe pattern Malkio's
  account had (~731 rows, ~650 dupes of one Chrome install, caused by the extension-side `local_id`
  regeneration bug fixed in 6.7.46 / `98c9e5b`). Run a one-off cleanup against Regina's `profile_id` only.
- **Correction to the ask's premise:** I could not find a git-tracked record of an already-executed
  *server-side* cleanup from 2026-07-20 to replicate verbatim — `docs/progress.md` has no entry naming
  Regina or a completed server-side dedup on that date. What **does** exist and is git-tracked is the
  **client-side** selection pattern in `sidecar/src/components/DevicesCard.tsx` (commit `958c814`,
  `groupKey`/`groupRows`/`isDefaultVisible`): group by `machine_id` when set, else `browser`+`local_id`
  prefix, else the row's own id; keep the most-recently-seen row per group. That same commit's comments
  explicitly say "a separate diagnosis task owns any actual cleanup" and "server-side cleanup ...
  follows separately" — i.e. as of 2026-07-20 the server-side deletion had **not yet landed** anywhere
  I can find. Builder should first grep Supabase SQL history / ask Malkio directly whether a script was
  run outside git before re-deriving one from scratch.
- **Fix (if no prior script is found):** SQL against Regina's `profile_id`, service-role, dry-run first:
  keep a row if (a) it's referenced by FK from `focus_items`/`clock_sessions`/other owner tables, (b) it
  has a non-null `display_name`, (c) it has a live/recent `auth_session_id`, or (d) it's the
  most-recently-seen row within its `machine_id`/`local_id` group (mirrors `groupKey` above). Delete the
  rest. Export a pre-delete row count + backup (CSV or a `_deleted` shadow table) as evidence.
- **Files:** new one-off script under `scripts/` or a scratch SQL file (not a numbered migration —
  this is data cleanup, not schema); no `src/` changes.
- **Size:** S.
- **Surfaces touched:** Supabase DB only (service-role), Regina's `profile_id` scope.
- **Verification gate:** before/after row count for Regina's `profile_id`; confirm her Devices card /
  Team Activity chip list renders a sane device count post-cleanup; confirm no FK constraint errors.
- **Dependencies:** none blocking. Informs TR-13 (Team Activity granularity) — do TR-01 first so TR-13's
  UI fix isn't validated against already-corrupted data for this account.
- **Owner suggestion:** whoever owns Supabase migrations this cycle (same persona as SYNTHESIS #2 was
  assigned to) — service-role SQL, needs care.

### TR-02. Companion 0.3.8 fix pass (batch of 3)
- **Source:** SYNTHESIS #3, `feat/companion-ux-wave` (`tabatha-desktop`, HEAD `72cddd9`).
- **What:** (1) CSS truncation swap regression — `.app-title`/`.app-name` ellipsis rule is on the wrong
  element post-refactor (`App.jsx:279-280` vs `styles/index.css:173-184`). (2) "Exit Completely" has zero
  confirmation dialog (`CompanionMenu.jsx:85,143` → `main.rs:527-531` `app.exit(0)` direct). (3) "Pair
  Extension" silently rotates the pairing token with no "already connected?" check
  (`CompanionMenu.jsx:79-83/124`, doc comment at `main.rs:509-511` confirms rotation revokes the
  previous client). Item 3 is security-adjacent (silent revoke of a working connection).
- **Fix:** confirm-dialog wrapper for exit + pair-token rotate in `CompanionMenu.jsx`; swap the
  truncation rule in `styles/index.css`. No Rust changes needed.
- **Files:** `tabatha-desktop/src/CompanionMenu.jsx`, `tabatha-desktop/src/styles/index.css`.
- **Size:** S (single PR, 2 files).
- **Surfaces touched:** Desktop companion only.
- **Verification gate:** `npm run build` + `cargo check` clean (already confirmed clean pre-fix); manual
  click-through of both confirm dialogs; visually confirm long window titles truncate correctly.
- **Dependencies:** none. This is a release gate for the companion-update announcement — treat as
  blocking that announcement, not backlog.
- **Owner suggestion:** whoever picks up `feat/companion-ux-wave` to finish it before the announcement.

### TR-03. Gatekeeper race — intent gate renders after the page, not before
- **Source:** SYNTHESIS #12, Argus C1.
- **What:** `src/content/gatekeeper.js` builds the intent-gate overlay only after awaiting
  `CHECK_CONTEXT_NEEDED`/`GET_FOCUS_ENGINE` + two storage round-trips. `document_start` doesn't
  guarantee the page stays frozen during those awaits, so pages can render before the gate appears —
  undermining the core "set intent before browsing" mechanism.
- **Fix:** inject a cheap dimming placeholder synchronously first, swap in the full form once data
  resolves. Needs care to avoid a flash-of-placeholder feeling worse than the current race in the
  common fast-resolve case.
- **Files:** `src/content/gatekeeper.js`.
- **Size:** M.
- **Surfaces touched:** Extension content script (all pages).
- **Verification gate:** load unpacked, hard-navigate to a fresh domain with intent required, confirm
  no frame of un-gated content renders before the placeholder; confirm fast-resolve path doesn't
  visibly flicker placeholder→form.
- **Dependencies:** none. Can parallelize with TR-04 (different code path) but not with TR-13/other
  InBar-adjacent work without a rebase check — both touch content-script files.
- **Owner suggestion:** any agent comfortable with MV3 content-script timing.

### TR-04. BlockGate's first `sendMessage` has no try/catch
- **Source:** SYNTHESIS "also promoted," Argus C11.
- **What:** Unlike `gatekeeper.js`/`inbar.js`, BlockGate's first `sendMessage` call throws unhandled on
  an invalidated extension context — the block silently fails **open** (security-adjacent: a blocked
  site becomes reachable instead of staying blocked on error).
- **Fix:** one-line try/catch, fail closed (or to the existing fallback state) instead of throwing.
- **Files:** BlockGate content script (`src/content/` — same directory as gatekeeper.js/inbar.js).
- **Size:** S.
- **Surfaces touched:** Extension content script (BlockGate only).
- **Verification gate:** simulate an invalidated extension context (reload extension mid-navigation)
  and confirm the block still holds instead of silently opening the site.
- **Dependencies:** none.
- **Owner suggestion:** whoever does TR-03 — same file family, cheap to bundle in the same PR review
  even if it's a separate commit.

---

## Tier 2 — User-visible bugs

### TR-05. Watch: unguarded network calls crash the app
- **Source:** SYNTHESIS #4, Cirra §2 (Critical/BUG, not independently re-verified this pass but
  high-confidence per Koda — named classes and an internally-consistent contrast case).
- **What:** `SupabaseFocusRepository`/`PostgrestClient`/`GoTrueClient` calls in `tabatha-watch` aren't
  wrapped — any connectivity failure crashes the app. `PairViewModel.submit()` already does this
  correctly with `runCatching`, showing the fix pattern already exists in-repo.
- **Fix:** wrap every repository call site in `runCatching`, surface inline error state, keep
  last-known UI instead of crashing.
- **Files:** `tabatha-watch` — `SupabaseFocusRepository` and every `viewModelScope.launch{}` site
  without a `CoroutineExceptionHandler` (pause/resume/extend/resolve/addCheckpoint/loadFocusItems).
- **Size:** M (mechanical wrap, touches every call site).
- **Surfaces touched:** Watch app only.
- **Verification gate:** kill network mid-session on a real or emulated Watch, confirm no crash and an
  inline error/stale-state indicator instead.
- **Dependencies:** none. Ship-blocker for any Watch release regardless of what else lands.
- **Owner suggestion:** ship-blocker priority — whoever owns the next Watch release.

### TR-06. Home: Context Activity blank for companion-less users
- **Source:** SYNTHESIS #9, Argus H7 (high severity, plausible majority-of-users impact since the
  desktop companion is opt-in).
- **What:** `UnifiedTimeline` early-returns empty *before* folding in clock breaks/clock-in-out markers
  whenever `sessions.length === 0` — any user who's never connected the companion sees a fully blank
  "Context Activity" section, not even their own clock markers, with no empty-state explanation.
- **Fix:** build break/clock-marker segments independent of companion `sessions.length`; only
  early-return when truly nothing exists at all; add a distinct empty-state message for the true-empty
  case.
- **Files:** `UnifiedTimeline` component in `src/home/`.
- **Size:** S-M.
- **Surfaces touched:** Home page.
- **Verification gate:** load as a user with zero companion sessions but real clock history; confirm
  clock markers render; load as a genuinely-empty user; confirm the new empty-state message (not blank).
- **Dependencies:** none.
- **Owner suggestion:** any agent with Home-surface familiarity.

### TR-07. Break notes silently don't persist
- **Source:** SYNTHESIS #10, Argus W1 (high severity — the one place in the audit where the UI actively
  lies about whether input was saved).
- **What:** `BreakNotes` saves to local React state only; its own code comment says "In production this
  would persist via sendMessage."
- **Fix (pick one, either is acceptable tonight):** wire a real save handler into
  `clockService.js`/clock history (M), or add the same stub/"SOON" badge used elsewhere so the UI stops
  lying about save state until real persistence lands (S, fastest stopgap).
- **Files:** `BreakNotes` component, `clockService.js`.
- **Size:** S (stopgap) or M (real persistence) — default to the stopgap badge tonight unless the
  builder has clean runway for the real fix; either closes the "UI lies" problem.
- **Surfaces touched:** Home / clock UI.
- **Verification gate:** type a break note, reload the page/reopen the panel, confirm either (a) the
  note persists, or (b) a visible "not yet saved" badge is present so the user isn't misled.
- **Dependencies:** none.
- **Owner suggestion:** any agent.

### TR-08. Backburner-initiate button missing from Sidebar and Home
- **Source:** SYNTHESIS #11, Argus B1 (high severity — contradicts `docs/features/207-backburner.md`'s
  own spec: "Available on InBar, Homepage, and Sidebar," only InBar has it).
- **Note on the original seed ask:** Malkio's seed framing ("Focus can't be backburnered, only tabs —
  Sidecar can do both") is inverted from what's shipped and was already killed by the synthesis pass —
  Backburner is Focus-only everywhere, Sidecar has no tab concept. The real, concrete gap being queued
  here is the missing button on 2 of 3 promised surfaces, not the seed's literal framing.
- **Fix:** add a 🔥 button next to Pause in both action rows, wired to the same `BACKBURNER_FOCUS`
  message InBar already uses.
- **Files:** Sidebar and Home focus/queue action-row components.
- **Size:** S.
- **Surfaces touched:** Sidebar, Home.
- **Verification gate:** click 🔥 on Sidebar and Home, confirm the focus moves to backburner identically
  to the existing InBar behavior.
- **Dependencies:** none.
- **Owner suggestion:** any agent.

### TR-09. Pairing-code copy implies TV/Watch-only
- **Source:** SYNTHESIS "also promoted," Argus D4 / Cirra §1.1 (med).
- **What:** Copy hides that the pairing code is exactly what a signed-out phone needs too, not just
  TV/Watch.
- **Fix:** soften copy on both extension and Sidecar surfaces.
- **Size:** S.
- **Surfaces touched:** Extension pairing UI, Sidecar pairing UI.
- **Verification gate:** read the updated copy in both surfaces, confirm it no longer implies
  TV/Watch-exclusivity.
- **Dependencies:** none.
- **Owner suggestion:** any agent, copy-only change.

### TR-10. Popup search placeholder says wrong shortcut
- **Source:** SYNTHESIS "also promoted," Argus B9 (low).
- **What:** Placeholder says "Ctrl+Space," actual shortcut is Ctrl+Shift+E.
- **Fix:** one-line fix, or read `chrome.commands.getAll()` dynamically so it can't drift again.
- **Size:** S.
- **Surfaces touched:** Popup.
- **Verification gate:** open popup, confirm placeholder matches the real bound shortcut.
- **Dependencies:** none.
- **Owner suggestion:** any agent.

### TR-11. `SessionList.jsx` dead code removal
- **Source:** SYNTHESIS "also promoted," Argus H6 (low).
- **What:** Unreferenced component still in the tree.
- **Fix:** delete file + its import.
- **Size:** S.
- **Surfaces touched:** wherever it was imported (confirm zero remaining references before deleting).
- **Verification gate:** `npm run build` clean after removal; grep confirms no remaining imports.
- **Dependencies:** none.
- **Owner suggestion:** any agent — good filler task between larger items.

### TR-12. Live Preview blank for 11/24 settings sections
- **Source:** SYNTHESIS "also promoted," Argus S1 (high visibility).
- **What:** 11 of 24 settings sections show a blank Live Preview pane with no fallback message.
- **Fix (cheap half only, tonight):** add the existing generic fallback string everywhere it's missing.
  Building real mockups for Context View/Blocked Sites (S3/S4) is explicitly OVERHAUL-scoped — do not
  attempt that part tonight.
- **Files:** `src/settings/` (wherever Live Preview panes are registered per section).
- **Size:** S (fallback-message pass only).
- **Surfaces touched:** Settings.
- **Verification gate:** click through all 24 settings sections, confirm none render a bare blank pane.
- **Dependencies:** none.
- **Owner suggestion:** any agent.

---

## Tier 3 — Team-visibility features (Malkio's fresh asks)

### TR-13. Team Activity device-chip granularity fix
- **Source:** Malkio ask (a), 2026-07-22 (screenshot evidence: Regina showing ~30 "chrome · no status"
  duplicate chips).
- **What:** `src/settings/TeamActivityPanel.jsx:329` renders one raw chip per install row with a bare
  `"no status"` fallback when `StatusChip` has nothing to show — no grouping, no revoked/stale
  filtering. Confirmed by direct read: the fallback path is a plain `<span>no status</span>`, no
  dedup logic at all in this file.
- **Fix:** apply the same grouping/filtering pattern already shipped for the self-view in
  `sidecar/src/components/DevicesCard.tsx` (`groupKey`/`groupRows`/`isDefaultVisible`, commit `958c814`)
  to `TeamActivityPanel.jsx`'s per-member chip rendering: group by `machine_id` → `browser+local_id` →
  row id, show the most-recent per group, filter out revoked rows, and show a name/kind/live-status
  chip instead of raw "chrome · no status." This logic currently lives client-side in one component
  (Sidecar) and needs to be either shared (extracted to a util both surfaces import) or reimplemented
  consistently in the extension settings surface — extracting to a shared util is preferred to avoid
  the two-copies drift SYNTHESIS's OVERHAUL theme #3 already flags ("grouping logic duplicated, not
  shared, between extension and Sidecar").
- **Files:** `src/settings/TeamActivityPanel.jsx`; consider extracting grouping helpers to
  `src/utils/deviceGrouping.js` (new) shared by both this file and (eventually) `sidecar/src/components/DevicesCard.tsx`.
- **Size:** M (shared-util extraction + wiring into a second surface, plus revoked/stale filtering that
  doesn't exist there today).
- **Surfaces touched:** Extension Settings (Team Activity panel).
- **Verification gate:** load Team Activity for an account with a flooded device history (Regina's,
  post-TR-01 cleanup ideally) and confirm chips collapse to a sane per-person device count with
  readable names/kinds/live status, not a wall of duplicates.
- **Dependencies:** run after TR-01 (Regina's cleanup) so this is validated against real, not corrupted,
  data — though the fix itself doesn't strictly require the cleanup to land first.
- **Owner suggestion:** an agent already familiar with `DevicesCard.tsx`'s grouping logic (extraction
  work benefits from having written the original).

### TR-14. Feedback buttons on Sidecar, extension side panel, and Tabatha home
- **Source:** Malkio ask (d), 2026-07-22.
- **What:** Non-intrusive quick-feedback affordance on 3 surfaces that don't have one yet, sharing the
  existing bug/feature payload path (`feedback-to-asana` edge function, already deployed and CORS-scoped
  to the extension origin + `https://tabatha.pondocean.co` per `supabase/functions/feedback-to-asana/index.ts:24-27`).
- **Design constraint (explicit):** non-intrusive — a small persistent affordance (e.g. corner icon
  or settings-adjacent link), not a modal that interrupts flow, matching the existing bug/feature box's
  restraint.
- **Fix:** reuse `feedbackService.js`'s existing `{ kind, text, version, context }` contract; add the
  entry-point UI in 3 places: Sidecar (extend the existing Epic 7 feedback card if a lighter global
  affordance is wanted, or add alongside it), extension side panel (Sidebar), Tabatha home.
- **Files:** `src/sidebar/index.jsx`, `src/home/index.jsx`, `sidecar/src/screens/` (wherever a persistent
  low-chrome affordance fits per Sidecar's existing feedback card component).
- **Size:** M (3 surfaces, but reusing one payload path and one edge function — no backend work).
- **Surfaces touched:** Sidebar, Home, Sidecar.
- **Verification gate:** submit a test bug/idea from each of the 3 new surfaces, confirm each lands as
  a task in Asana project `1214031898449333` with correct `surface` context tag; confirm the affordance
  doesn't block or overlay primary content at rest.
- **Dependencies:** none technical. Sequencing note: land after TR-13 if the same agent is touching
  `TeamActivityPanel.jsx`/Sidebar files, purely to avoid two agents editing `src/sidebar/index.jsx` same
  night — otherwise independent.
- **Owner suggestion:** any agent; UI-only + one existing edge function, no new backend surface.

### TR-15. Feedback-review automation prompt/protocol doc
- **Source:** Malkio ask (e), 2026-07-22.
- **What:** Author the protocol/prompt doc for an every-6h agent that reviews new feedback (bug reports
  + feature requests posted by `feedback-to-asana`), vets each, plans or proposes a fix, and queues
  qualifying items for the nightly bug-fix TaskRun. CeeCee is separately scheduling the cron; this item
  is the doc only.
- **GID resolved during this queue-assembly pass:** the ask says to "find its GID from
  `supabase/functions/feedback-to-asana/index.ts`" — that file does **not** hardcode the GID (it reads
  `Deno.env.get("ASANA_PROJECT_GID")`, a Supabase secret, by design, per the file's own header comment
  about keeping the PAT/GID out of the world-readable unpacked extension). I resolved the live value
  empirically instead: `asana-cli.cmd task search --text "Submitted from Tabatha"` returns real and
  test feedback tasks (e.g. gid `1216713224519004`, `1216712694759006`, `1216712939534243`,
  `1216679002855862`) all filed under project **`1214031898449333`** — the same "Flux Development"
  project this queue's own tracking task lives in. Save the builder the re-discovery step.
- **Fix:** write `docs/taskrun/feedback-review-agent.md` covering: trigger cadence (6h), read scope
  (new/unactioned tasks in project `1214031898449333` matching the feedback-fn title pattern
  `🐛 [bug] …` / `💡 [idea] …`), vetting criteria (real vs. test/noise — note test tasks like the 4 GIDs
  above already exist and must be filtered out or completed/archived), triage output (plan a fix inline
  for small items, propose-only for large ones), and the hand-off contract into the nightly bug-fix
  TaskRun queue (same shape as this file).
- **Files:** new `docs/taskrun/feedback-review-agent.md`.
- **Size:** S (doc-writing only, no code).
- **Surfaces touched:** none (process doc).
- **Verification gate:** doc exists, names the correct project GID, and defines a concrete hand-off
  format the nightly TaskRun assembler (i.e., tomorrow's Rook-equivalent) can consume mechanically.
- **Dependencies:** none — can run fully in parallel with everything else in this queue.
- **Owner suggestion:** any agent; also worth flagging the 4 pre-existing test tasks in the project for
  cleanup/archival as part of writing this doc, so the review agent doesn't choke on them night one.

---

## Tier 4 — Hygiene

### TR-16. `site/docs` deploy lacks `--branch=main` flag
- **Source:** SYNTHESIS "also promoted," self-flagged already in `docs/OPERATIONS.md`.
- **What:** Preview-deploy risk on the docs Cloudflare Pages deploy command.
- **Fix:** one-line wrangler flag addition.
- **Size:** S.
- **Surfaces touched:** deploy tooling only.
- **Verification gate:** next docs deploy targets the production branch, not a preview URL.
- **Dependencies:** none.
- **Owner suggestion:** any agent with pondo-show-launch familiarity.

### TR-17. Sidecar `SIDECAR_VERSION` hardcoded independent of `app.json`
- **Source:** SYNTHESIS "also promoted," Rook #21 (latent).
- **What:** Version string duplicated instead of generated at build time.
- **Fix:** generate from `app.json` at build, same pattern as the extension's `sync-version.mjs`.
- **Size:** S.
- **Surfaces touched:** Sidecar build tooling.
- **Verification gate:** bump `app.json` version, confirm the in-app display updates without a manual
  second edit.
- **Dependencies:** loosely related to TR-19 (version reconciliation) — fine to do independently, but
  don't let it fight TR-19 over the same build script in the same night; sequence after if same agent.
- **Owner suggestion:** any agent.

### TR-18. Git-line reconciliation prep (staging/origin/integrate/6.7.50 divergence)
- **Source:** open reconciliation chore (CeeCee brief) + findings from this queue-assembly pass.
- **What, confirmed by direct inspection tonight:**
  - Local `staging` (`211a398`) is **12 commits ahead of `origin/staging`** — includes the 6.7.52
    through 6.7.56 release commits (device pairing-code mint, session-aware reclaim, XSS sweep, RLS
    guard) that GitHub doesn't have yet. This is a straightforward fast-forward push once approved —
    not a real divergence/conflict.
  - `integrate/6.7.50` (`07866c2`, pushed to origin, **not merged into staging**) contains
    "fix(security): RLS state-aware guard on browser_profiles.revoked_at (audit NOW #2)" — the same fix
    that shipped independently and directly on `staging` as part of 6.7.54→6.7.56. This branch is very
    likely fully superseded; needs a diff check before deleting, not an assumption.
  - `feat/logo-rollout` (`bf08fcc`) and `feat/ext-device-management` (`2c6e446`) are also **not merged
    into staging** — confirmed via `git merge-base --is-ancestor`. `feat/ext-device-management` should
    be diffed against staging's already-shipped device-management work (6.7.44 line, #222 docs) to
    determine if it's still-needed or superseded before any merge decision.
  - Several `claude/*` branches (`claude/epic3-asana-sync`, `claude/epic8-nudges`,
    `claude/sidecar-lane-a-chunk2`, `claude/sidecar-notes-simple`) show their upstream tracking
    pointed at `origin/claude/tabby-sidecar-mobile-46c612` (this session's own branch) instead of their
    own origin ref — a mis-tracked-upstream git-config artifact from worktree creation, not a real
    content problem, but confusing for anyone running `git branch -vv`. Cosmetic fix: `git branch
    --set-upstream-to` each to its correct origin branch.
- **Fix (prep only, no push to protected branches):** (1) diff `integrate/6.7.50` against staging to
  confirm full supersession, then propose closing it; (2) diff `feat/ext-device-management` against
  staging's shipped device-mgmt work; (3) fix the 4 mis-tracked upstreams; (4) prepare (do not execute)
  the `origin/staging` fast-forward push as a reviewable diff for Malkio/CeeCee sign-off.
- **Files:** none (git operations + a short reconciliation note, not code).
- **Size:** M (mostly investigation + safe git hygiene, bounded).
- **Surfaces touched:** none.
- **Verification gate:** a short reconciliation note (append to `docs/progress.md` or this queue's
  follow-up) listing which branches are confirmed superseded/safe-to-close vs. still-needed, plus
  confirmation the 4 upstream-tracking fixes applied cleanly.
- **Dependencies:** the actual `origin/staging` push and any branch deletions need explicit
  human/CeeCee approval per Global Rule 3 — this item preps the ground, it does not execute the push.
  See Excluded section below.
- **Owner suggestion:** an agent comfortable with git archaeology; not a builder task.

### TR-19. Version/logo drift reconciliation + docs refresh (sequenced pair)
- **Source:** SYNTHESIS #8 (version/logo drift) + #7 (docs staleness), explicitly sequenced together
  by Koda: "do #8 first (source-of-truth fix), then #7 (which references the now-correct version)."
- **What:** `staging` still ships the old "Split-Tab T" logo while `feat/logo-rollout` sits unmerged
  (confirmed unmerged tonight, see TR-18); Sidecar PWA icons (192/512) are stale since v0.1.0; the site
  favicon cascade needs regeneration since Koda's prior generation attempt died. Separately,
  `site/docs/index.html:60,189` hardcodes `v6.7.41` and 8 shipped features are missing/stale in docs
  content (device management panel, extension-minted pairing codes, count direction/precision,
  un-resolve, phone-away 3-way heartbeat semantics, invites, TV sign-in, PWA orientation fix).
- **Fix:** merge `feat/logo-rollout` into `staging` (after the TR-18 diff confirms it's still the
  intended asset — do not merge blind); regenerate Sidecar PWA icons + site favicon cascade from the
  current mark; drive the docs verbadge from `manifest.json` at build time (same pattern as
  `sync-version.mjs`) instead of hand-maintaining it; content-rewrite pass on `pairing-devices.html`,
  `phone-focus-mode.html`, `timers-extensions-backburner.html`, `focus-and-intents.html`, plus new
  sections for invites and TV sign-in; update `Tabatha_Changelog.md`'s top entry (currently stuck at
  v6.5.0, dated 2026-07-01 per this repo's own `AGENTS.md` "Current version" line — itself stale).
- **Files:** `public/icons/*`, `sidecar/public/icons/*`, `sidecar/public/manifest.webmanifest`,
  `Tabatha_Changelog.md`, `site/docs/index.html` + 4 named pages, `scripts/sync-version.mjs` (or a new
  docs-specific build step).
- **Size:** S-M (logo/icon half) + M (docs content half) — boundable to one agent-day total, but do not
  split across two agents touching the same files concurrently (Koda's own sequencing warning).
- **Surfaces touched:** icons/assets, site docs, changelog.
- **Verification gate:** `staging` renders the new mark everywhere (extension icon, Sidecar PWA icon,
  site favicon); docs verbadge matches `manifest.json` version automatically after a version bump;
  the 4 named doc pages + invites/TV-sign-in sections reflect currently-shipped behavior.
- **Dependencies:** confirm via TR-18 that `feat/logo-rollout` is still the correct/current asset before
  merging (worktree-vs-worktree disagreement was explicitly flagged by Koda) — see Excluded section for
  the one open question that needs Malkio, not a blocker on starting the icon/favicon regen work itself.
- **Owner suggestion:** whoever's doing release hygiene this cycle; do this before any CWS store
  submission, not after.

---

## Excluded — needs Malkio's input (route to morning-questions list, not tonight's queue)

- **Org-hours v1 RPC (SYNTHESIS #5)** — the technical shape is ready (`019_owner_read_views.sql`
  already computes the right aggregates, currently `service_role`-only by deliberate design), but Koda's
  own synthesis explicitly says this should be sequenced **after Malkio confirms the opt-in
  default/consent language** in the #221 concept doc
  (`docs/superpowers/specs/2026-07-21-shared-focus-org-context-concept.md`), not built ahead of that
  confirmation. **Morning question:** does Malkio want to lock the #221 consent model (aggregate-only
  default, per-person opt-in via `profiles.settings.share_hours_with_org`) tonight so this can build
  tomorrow, or is there a different default he wants first?

- **Team worked stints in Work Shifts (Malkio ask b)** — explicitly sequenced by Malkio himself
  ("RPC first, then both UIs eat it") behind the org-hours RPC above. Blocked transitively on the same
  question. No separate morning question needed — resolves automatically once org-hours is unblocked.

- **Email/SMTP branding change (SYNTHESIS #6)** — root-caused to unconfigured custom SMTP; the plan is
  fully written and ready to execute the moment the credential exists, but step 1 is literally "Malkio
  generates an app password for `tabatha@duckandshark.com` at myaccount.google.com/apppasswords."
  Zero code changes required elsewhere. **Morning question:** can Malkio generate and hand over that
  app password? Everything else in this item can then run same-day.

- **`origin/staging` push + closing `integrate/6.7.50`/stale branches (part of TR-18)** — TR-18 preps
  the diffs and confirms what's safe, but the actual `git push origin staging` (12 commits, includes
  security-relevant 6.7.56 fixes) and any branch deletions are protected-branch actions per Global
  Rule 3. **Morning question:** approve pushing local `staging` to `origin/staging` as a straight
  fast-forward (no new content, just catching GitHub up), and approve closing `integrate/6.7.50` once
  TR-18's diff confirms full supersession?

- **`feat/logo-rollout` merge target confirmation (part of TR-19)** — Koda's synthesis flagged that
  which worktree is the "intended CWS-submission source" is itself in question given the
  GitHub/local/worktree version disagreement documented in this repo's own audit history.
  **Morning question:** confirm `staging` is the correct merge target and CWS-submission source before
  TR-19's logo merge executes (the icon/favicon regeneration work can start independent of this answer;
  only the final merge-into-staging step needs it).

- **staging → main promotion** — not queued as an executable item tonight. Staging currently carries
  security-relevant unreleased fixes (6.7.56 XSS/RLS work) plus 12 unpushed-to-GitHub commits; a full
  production promotion is exactly the kind of protected-branch, human-approval-gated action Global
  Rule 3 exists for, and it should follow (not race ahead of) the `origin/staging` push question above.
  **Morning question:** once `origin/staging` is caught up (see above), does Malkio want a
  staging→main promotion PR prepared for review, and on what timeline?
